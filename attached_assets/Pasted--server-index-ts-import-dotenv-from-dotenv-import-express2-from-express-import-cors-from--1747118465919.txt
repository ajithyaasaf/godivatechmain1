// server/index.ts
import dotenv from "dotenv";
import express2 from "express";
import cors from "cors";

// server/routes.ts
import { createServer } from "http";
import { WebSocketServer, WebSocket } from "ws";

// shared/schema.ts
import { pgTable, text, serial, integer, boolean, timestamp } from "drizzle-orm/pg-core";
import { createInsertSchema } from "drizzle-zod";
var users = pgTable("users", {
  id: serial("id").primaryKey(),
  username: text("username").notNull().unique(),
  password: text("password").notNull()
});
var insertUserSchema = createInsertSchema(users).pick({
  username: true,
  password: true
});
var categories = pgTable("categories", {
  id: serial("id").primaryKey(),
  name: text("name").notNull().unique(),
  slug: text("slug").notNull().unique()
});
var insertCategorySchema = createInsertSchema(categories).pick({
  name: true,
  slug: true
});
var blogPosts = pgTable("blog_posts", {
  id: serial("id").primaryKey(),
  title: text("title").notNull(),
  slug: text("slug").notNull().unique(),
  excerpt: text("excerpt").notNull(),
  content: text("content").notNull(),
  published: boolean("published").notNull().default(true),
  authorName: text("author_name").notNull(),
  authorImage: text("author_image"),
  coverImage: text("cover_image"),
  publishedAt: timestamp("published_at").notNull().defaultNow(),
  categoryId: integer("category_id").references(() => categories.id)
});
var insertBlogPostSchema = createInsertSchema(blogPosts).pick({
  title: true,
  slug: true,
  excerpt: true,
  content: true,
  published: true,
  authorName: true,
  authorImage: true,
  coverImage: true,
  publishedAt: true,
  categoryId: true
});
var testimonials = pgTable("testimonials", {
  id: serial("id").primaryKey(),
  name: text("name").notNull(),
  position: text("position").notNull(),
  company: text("company").notNull(),
  content: text("content").notNull(),
  image: text("image")
});
var insertTestimonialSchema = createInsertSchema(testimonials).pick({
  name: true,
  position: true,
  company: true,
  content: true,
  image: true
});
var projects = pgTable("projects", {
  id: serial("id").primaryKey(),
  title: text("title").notNull(),
  description: text("description").notNull(),
  fullDescription: text("full_description"),
  // Detailed project description
  image: text("image"),
  gallery: text("gallery").array(),
  // Additional images for project gallery
  category: text("category").notNull(),
  technologies: text("technologies").array().notNull(),
  link: text("link"),
  githubLink: text("github_link"),
  // Link to GitHub repository
  clientName: text("client_name"),
  // Client or company name
  completionDate: text("completion_date"),
  // When the project was completed
  featured: boolean("featured").default(false),
  // Whether to show as featured project
  testimonial: text("testimonial"),
  // Client testimonial about the project
  challengeDescription: text("challenge_description"),
  // Description of challenges faced
  solutionDescription: text("solution_description"),
  // Description of solutions implemented
  resultsDescription: text("results_description"),
  // Description of results achieved
  order: integer("order").default(0)
  // For manual ordering of projects
});
var insertProjectSchema = createInsertSchema(projects).pick({
  title: true,
  description: true,
  fullDescription: true,
  image: true,
  gallery: true,
  category: true,
  technologies: true,
  link: true,
  githubLink: true,
  clientName: true,
  completionDate: true,
  featured: true,
  testimonial: true,
  challengeDescription: true,
  solutionDescription: true,
  resultsDescription: true,
  order: true
});
var services = pgTable("services", {
  id: serial("id").primaryKey(),
  title: text("title").notNull(),
  description: text("description").notNull(),
  icon: text("icon").notNull(),
  slug: text("slug").notNull().unique()
});
var insertServiceSchema = createInsertSchema(services).pick({
  title: true,
  description: true,
  icon: true,
  slug: true
});
var teamMembers = pgTable("team_members", {
  id: serial("id").primaryKey(),
  name: text("name").notNull(),
  position: text("position").notNull(),
  bio: text("bio").notNull(),
  image: text("image"),
  linkedIn: text("linkedin"),
  twitter: text("twitter")
});
var insertTeamMemberSchema = createInsertSchema(teamMembers).pick({
  name: true,
  position: true,
  bio: true,
  image: true,
  linkedIn: true,
  twitter: true
});
var contactMessages = pgTable("contact_messages", {
  id: serial("id").primaryKey(),
  name: text("name").notNull(),
  email: text("email").notNull(),
  phone: text("phone"),
  subject: text("subject").notNull(),
  message: text("message").notNull(),
  createdAt: timestamp("created_at").notNull().defaultNow()
});
var insertContactMessageSchema = createInsertSchema(contactMessages).pick({
  name: true,
  email: true,
  phone: true,
  subject: true,
  message: true
});
var subscribers = pgTable("subscribers", {
  id: serial("id").primaryKey(),
  email: text("email").notNull().unique(),
  createdAt: timestamp("created_at").notNull().defaultNow()
});
var insertSubscriberSchema = createInsertSchema(subscribers).pick({
  email: true
});

// client/src/lib/sampleData.ts
var sampleCategories = [
  { id: 1, name: "Web Development", slug: "web-development" },
  { id: 2, name: "Digital Marketing", slug: "digital-marketing" },
  { id: 3, name: "Mobile App Development", slug: "mobile-app-development" },
  { id: 4, name: "Design & Branding", slug: "design-branding" },
  { id: 5, name: "Business Tips", slug: "business-tips" }
];
var sampleBlogPosts = [
  {
    id: 1,
    title: "5 Reasons Why Your Business Needs a Professional Website in 2023",
    slug: "why-business-needs-professional-website",
    excerpt: "Discover why having a professionally designed website is essential for businesses in Madurai to attract customers and grow their brand.",
    content: "In today's digital-first world, having a professional website is no longer optional for businesses in Madurai\u2014it's essential. At GodivaTech, we've helped numerous local businesses establish their online presence, and we've seen firsthand how a well-designed website can transform a business.\n\nHere are five compelling reasons why your business needs a professional website in 2023:\n\n1. **Establishing Credibility and Trust**: When potential customers search for your products or services online, a professional website instantly communicates reliability and legitimacy. Without an online presence, you risk losing customers to competitors who appear more established and trustworthy simply because they have a professional website.\n\n2. **24/7 Accessibility**: Unlike a physical store with set business hours, your website works around the clock. It provides potential customers with information about your products or services, answers common questions, and even processes sales or bookings at any time of day or night, serving customers when it's most convenient for them.\n\n3. **Expanding Your Reach**: A website dramatically extends your business's reach beyond Madurai. While local customers remain your core audience, a website allows you to connect with potential customers across Tamil Nadu, India, and even globally, depending on your business model.\n\n4. **Cost-Effective Marketing**: Compared to traditional advertising channels like newspaper ads or billboards, a website provides exceptional ROI. Once established, your ongoing costs are minimal while your website continues to work as your digital storefront, salesperson, and customer service representative.\n\n5. **Competitive Advantage**: In many industries in Madurai, businesses are still catching up with digital transformation. Having a professional, mobile-friendly website with smooth user experience can give you a significant edge over competitors who either don't have a website or have an outdated one.\n\nAt GodivaTech, we specialize in creating affordable, high-quality websites tailored specifically for Madurai businesses. Our web development team understands the local market and can help you establish a strong online presence that resonates with your target audience while reflecting your brand values.",
    publishedAt: "2023-10-15T00:00:00.000Z",
    coverImage: "https://images.unsplash.com/photo-1563986768609-322da13575f3?ixlib=rb-1.2.1&auto=format&fit=crop&w=800&q=80",
    authorName: "Ananth",
    authorImage: "https://randomuser.me/api/portraits/men/76.jpg",
    categoryId: 1
  },
  {
    id: 2,
    title: "How to Choose the Right Digital Marketing Strategy for Your Small Business",
    slug: "digital-marketing-strategy-small-business",
    excerpt: "Learn which digital marketing approaches work best for small businesses in Madurai and how to create a strategy that maximizes your marketing budget.",
    content: "Digital marketing offers tremendous opportunities for small businesses in Madurai, but with so many platforms and strategies available, it can be challenging to know where to invest your limited marketing budget. At GodivaTech, we've helped numerous local businesses develop effective digital marketing strategies that deliver real results.\n\nHere's our guide to choosing the right digital marketing approach for your small business:\n\n1. **Define Clear Goals**: Before selecting marketing channels, clearly define what you want to achieve. Are you looking to increase brand awareness, generate leads, drive website traffic, or boost online sales? Different goals require different strategies, so clarity here is essential.\n\n2. **Know Your Audience**: Understanding exactly who your customers are is crucial for effective digital marketing. Consider demographics like age, location, and income, but also dig deeper into their interests, pain points, and online behaviors. This knowledge will help you select the right platforms and create resonant messaging.\n\n3. **Assess Your Budget Realistically**: Digital marketing doesn't have to be expensive, but you need to be realistic about what you can achieve with your budget. For most small businesses in Madurai, it's better to excel on one or two channels than to spread resources too thinly across many platforms.\n\n4. **Consider These Effective Channels**:\n   - **Google My Business**: Essential for local businesses and completely free. An optimized listing helps you appear in local searches and Google Maps.\n   - **Social Media Marketing**: Facebook and Instagram are particularly effective in Madurai. Choose platforms where your audience spends time.\n   - **Content Marketing**: Blog posts that answer common customer questions can drive organic traffic and establish your expertise.\n   - **Email Marketing**: Still offers one of the highest ROIs for small businesses and is perfect for nurturing relationships with existing customers.\n\n5. **Start Small and Scale**: Begin with one or two channels, measure results, and refine your approach before expanding. This iterative approach allows you to learn what works for your specific business without overwhelming your resources.\n\nAt GodivaTech, we specialize in creating customized digital marketing strategies that work within your budget constraints while delivering measurable results. Our team understands the unique characteristics of the Madurai market and can help your business stand out in an increasingly competitive digital landscape.",
    publishedAt: "2023-09-22T00:00:00.000Z",
    coverImage: "https://images.unsplash.com/photo-1432888622747-4eb9a8f5f01a?ixlib=rb-1.2.1&auto=format&fit=crop&w=800&q=80",
    authorName: "Priya Ramanathan",
    authorImage: "https://randomuser.me/api/portraits/women/22.jpg",
    categoryId: 2
  },
  {
    id: 3,
    title: "Mobile App vs. Responsive Website: Which is Right for Your Business?",
    slug: "mobile-app-vs-responsive-website",
    excerpt: "Understand the differences between mobile apps and responsive websites to determine the best mobile strategy for your business needs.",
    content: "As smartphone usage continues to dominate internet access, businesses in Madurai are increasingly focusing on their mobile strategy. At GodivaTech, we're often asked by our clients whether they should invest in a responsive website or a dedicated mobile app. The answer depends on your specific business goals, budget, and target audience.\n\n**Understanding Responsive Websites**\n\nA responsive website automatically adjusts its layout and content to fit different screen sizes, from desktop monitors to smartphones. When properly designed, responsive websites provide an optimal viewing experience across all devices.\n\n**Key advantages of responsive websites:**\n\n1. **Cost-Effective Development**: Building and maintaining one responsive website is typically less expensive than developing separate mobile apps for different platforms (iOS, Android).\n\n2. **Immediate Accessibility**: Users can access your website instantly through their mobile browsers without having to download and install an app.\n\n3. **Easier Updates**: Changes and updates are implemented once and immediately available to all users, without requiring them to download updates.\n\n4. **Broader Reach**: Responsive websites work across all devices and platforms, reaching a wider audience without barriers to access.\n\n**Understanding Mobile Apps**\n\nMobile apps are standalone applications designed specifically for mobile devices and installed directly on the device through an app store.\n\n**Key advantages of mobile apps:**\n\n1. **Enhanced Performance**: Apps can operate faster than websites, with the ability to store data locally for offline access.\n\n2. **Advanced Functionality**: Apps can access device features like camera, GPS, push notifications, and contacts more seamlessly than websites.\n\n3. **Better User Experience**: Apps can offer more intuitive interfaces specifically designed for touch interactions and mobile use patterns.\n\n4. **Brand Presence**: Having your app icon on a user's home screen provides constant visibility and easier repeat engagement.\n\n**Making the Right Choice for Your Business**\n\nFor most small to medium businesses in Madurai, we recommend starting with a responsive website before considering a mobile app investment. This approach allows you to establish your online presence cost-effectively while reaching the widest possible audience.\n\nConsider a mobile app when:\n- Your business requires frequent customer engagement (like food delivery or ride services)\n- You need access to specific device features for core functionality\n- Your analytics show a substantial portion of repeat visitors using mobile devices\n- You have the budget for both initial development and ongoing maintenance\n\nAt GodivaTech, we can help you evaluate your specific needs and develop either a responsive website or mobile app that aligns with your business goals and budget. Our team specializes in creating mobile solutions that provide excellent user experiences while meeting your strategic objectives.",
    publishedAt: "2023-08-18T00:00:00.000Z",
    coverImage: "https://images.unsplash.com/photo-1526406915894-7bcd65f60845?ixlib=rb-1.2.1&auto=format&fit=crop&w=800&q=80",
    authorName: "Raj Sharma",
    authorImage: "https://randomuser.me/api/portraits/men/38.jpg",
    categoryId: 3
  },
  {
    id: 4,
    title: "7 Essential Elements of Effective Logo Design for Small Businesses",
    slug: "essential-elements-logo-design",
    excerpt: "Learn the key principles that make a logo design effective for your small business brand identity and how professional design can impact your success.",
    content: "Your logo is often the first visual element potential customers associate with your business. At GodivaTech, we've designed logos for numerous businesses in Madurai, and we've seen firsthand how a professionally designed logo can elevate a brand's perception and recognition. Here are seven essential elements that contribute to an effective logo design for small businesses:\n\n1. **Simplicity**: The most memorable logos are often the simplest. A clean, uncluttered design ensures your logo is easily recognizable and versatile across various applications, from business cards to billboards. Simple doesn't mean boring \u2013 it means focusing on what truly represents your brand without unnecessary complexity.\n\n2. **Relevance**: Your logo should reflect your industry and appeal to your target audience while communicating your brand's values and personality. A logo for a children's toy store will have a vastly different feel than one for a legal firm, and rightfully so.\n\n3. **Memorability**: An effective logo makes an immediate impression and stays in the viewer's mind. Often, distinctive shapes or unique twists on familiar elements can make your logo more memorable without becoming complicated.\n\n4. **Timelessness**: While following design trends might seem appealing, a truly effective logo stands the test of time. We design logos that won't look dated in a few years, saving you from costly rebranding efforts in the near future.\n\n5. **Versatility**: Your logo will appear in many different contexts \u2013 on your website, social media profiles, business cards, signage, and potentially on merchandise. A well-designed logo works effectively in color and black and white, and remains recognizable whether it's displayed large on a billboard or small on a business card.\n\n6. **Appropriate Color Psychology**: Colors evoke specific emotions and associations. Our designers carefully select colors that not only look appealing but also communicate the right message for your brand. For example, blue often conveys trustworthiness and professionalism, while orange suggests enthusiasm and creativity.\n\n7. **Distinctiveness**: In a competitive marketplace, your logo needs to stand out from competitors. This doesn't mean it needs to be radical \u2013 sometimes subtle distinctiveness within your industry's visual language can be more effective than something completely unrelated to your field.\n\nAt GodivaTech, our logo design process begins with understanding your business values, target audience, and competition. We then create multiple concepts, refine the selected direction, and deliver a complete package of logo files optimized for various uses. Our goal is to provide you with a logo that not only looks professional but also effectively communicates your brand's unique value proposition to potential customers in Madurai and beyond.",
    publishedAt: "2023-07-12T00:00:00.000Z",
    coverImage: "https://images.unsplash.com/photo-1585184394271-4c0a47dc59c9?ixlib=rb-1.2.1&auto=format&fit=crop&w=800&q=80",
    authorName: "Lakshmi Venkatesh",
    authorImage: "https://randomuser.me/api/portraits/women/32.jpg",
    categoryId: 4
  },
  {
    id: 5,
    title: "5 Social Media Strategies for Local Businesses in Madurai",
    slug: "social-media-strategies-local-businesses-madurai",
    excerpt: "Discover effective social media marketing approaches specifically tailored for Madurai-based businesses to increase local customer engagement.",
    content: "Social media has transformed how local businesses connect with customers, and businesses in Madurai can leverage these platforms to boost visibility and drive growth. At GodivaTech, we've helped numerous local businesses develop effective social media strategies tailored to the Madurai market.\n\nHere are five proven social media strategies for local businesses in Madurai:\n\n1. **Prioritize Platforms Where Your Customers Are Active**\n\nNot all social media platforms will deliver equal results for your business. Our market research shows that Facebook and Instagram remain the most effective platforms for reaching local customers in Madurai, with WhatsApp Business also providing excellent engagement opportunities. For businesses targeting younger demographics, platforms like Instagram Reels can be particularly effective.\n\nRather than stretching your resources across multiple platforms, focus on creating high-quality content for 1-2 platforms where your target audience is most active.\n\n2. **Showcase Local Relevance and Cultural Connection**\n\nOne advantage local businesses have over national brands is their connection to the community. Highlight your Madurai roots by sharing content that resonates with local culture and traditions. This might include:\n\n- Photos of your business at local landmarks or events\n- Posts celebrating local festivals like Pongal and Chithirai\n- Content featuring local dialect or references\n- Partnerships with other local businesses\n\nWhen customers see your authentic connection to Madurai, they're more likely to develop loyalty and trust in your brand.\n\n3. **Use Geo-Targeted Advertising for Better ROI**\n\nSocial media platforms offer powerful location-based targeting options that allow you to focus your advertising budget specifically on potential customers in Madurai and surrounding areas. Even with a modest budget, geo-targeted ads can deliver excellent results by reaching users based on their precise location.\n\nConsider creating special offers exclusively for local customers and promoting them through geo-targeted ads to drive foot traffic to your physical location.\n\n4. **Create and Share User-Generated Content**\n\nEncourage your customers to create content featuring your products or services. This might include:\n\n- Running photo contests where customers share their experiences\n- Offering discounts for customers who tag your business in posts\n- Showcasing customer testimonials with their permission\n\nUser-generated content builds trust through social proof and expands your reach as customers share their experiences with their own networks.\n\n5. **Maintain Consistent and Responsive Engagement**\n\nSocial media success requires more than just posting content\u2014it demands active engagement with your audience. Respond promptly to comments and messages, thank customers for positive feedback, and address concerns professionally and transparently.\n\nConsistency is also crucial. Develop a posting schedule that you can maintain long-term, as sporadic activity can negatively impact algorithm performance and audience engagement.\n\nAt GodivaTech, our digital marketing team can help you implement these strategies effectively, from creating compelling content to managing your social media presence. We understand the unique characteristics of the Madurai market and can help your business build a social media presence that drives real business results.",
    publishedAt: "2023-06-07T00:00:00.000Z",
    coverImage: "https://images.unsplash.com/photo-1611162617213-7d7a39e9b1d7?ixlib=rb-1.2.1&auto=format&fit=crop&w=800&q=80",
    authorName: "Vijay Kumar",
    authorImage: "https://randomuser.me/api/portraits/men/44.jpg",
    categoryId: 2
  },
  {
    id: 6,
    title: "How to Choose Colors for Your Business Website: A Guide for Non-Designers",
    slug: "choosing-website-colors-business-guide",
    excerpt: "Learn how to select the right color palette for your business website to create the right impression and improve user experience without design expertise.",
    content: "Color selection might seem like a subjective aesthetic choice, but it plays a crucial role in how customers perceive your business online. At GodivaTech, we've designed websites for numerous businesses in Madurai, and we've seen how the right color palette can significantly impact user engagement and conversion rates.\n\nHere's our practical guide to choosing effective website colors, especially for business owners without design expertise:\n\n1. **Start With Your Brand Colors**\n\nIf you already have an established logo or brand identity, begin with those colors as your foundation. Consistency between your offline materials and website reinforces brand recognition. If you don't have established brand colors yet, consider what colors might best represent your business personality and industry.\n\n2. **Understand Color Psychology for Business**\n\nDifferent colors evoke specific emotional responses and associations. While these associations can vary slightly across cultures, some general principles apply:\n\n- **Blue**: Communicates professionalism, trustworthiness, and reliability. Excellent for financial services, healthcare, and technology businesses.\n- **Green**: Suggests growth, health, and environmental consciousness. Ideal for healthcare, agriculture, and eco-friendly businesses.\n- **Red**: Creates a sense of urgency, energy, and passion. Works well for sales, food businesses, and brands wanting to stand out boldly.\n- **Orange**: Conveys enthusiasm, creativity, and affordability. Good for businesses appealing to younger audiences or promoting special offers.\n- **Purple**: Associated with luxury, creativity, and wisdom. Suitable for premium brands or creative services.\n- **Yellow**: Represents optimism, clarity, and warmth. Can work well for businesses wanting to appear accessible and cheerful.\n- **Black**: Portrays sophistication, luxury, and authority. Excellent for premium brands or modern, minimalist designs.\n\n3. **Create a Balanced Color Scheme**\n\nA professional website typically uses 2-3 primary colors plus neutral tones like white, black, or gray. We recommend following this structure:\n\n- **Primary color** (60%): Your main brand color, used for primary elements like header backgrounds or key buttons\n- **Secondary color** (30%): Complementary to your primary color, used for accents and secondary elements\n- **Accent color** (10%): Used sparingly for calls-to-action and elements you want to highlight\n\n4. **Consider Accessibility and Readability**\n\nEnsure there's sufficient contrast between your text and background colors to make content easily readable, especially for visitors with visual impairments. The Web Content Accessibility Guidelines (WCAG) recommend a contrast ratio of at least 4.5:1 for normal text.\n\n5. **Test Your Colors in Context**\n\nBefore finalizing your color scheme, test how it appears across different elements of your website\u2014headers, buttons, backgrounds, and text. What looks good as a small accent might be overwhelming when used for larger areas.\n\n6. **Research Competitors but Differentiate**\n\nLook at what colors competitors in your industry are using. While you want to meet industry expectations, also look for opportunities to differentiate your brand with a distinctive color approach.\n\nAt GodivaTech, our design team can help you select and implement the perfect color palette for your website that aligns with your brand values while maximizing user engagement and conversion. We understand both the technical aspects of color implementation and the psychological impact of different color choices on your target audience.",
    publishedAt: "2023-05-02T00:00:00.000Z",
    coverImage: "https://images.unsplash.com/photo-1523961131990-5ea7c61b2107?ixlib=rb-1.2.1&auto=format&fit=crop&w=800&q=80",
    authorName: "Ananth",
    authorImage: "https://randomuser.me/api/portraits/men/76.jpg",
    categoryId: 4
  }
];
var sampleServices = [
  {
    id: 1,
    title: "Web Development",
    description: "Create affordable, responsive websites for your business that work on all devices and help your brand stand out online.",
    icon: "code",
    slug: "web-development"
  },
  {
    id: 2,
    title: "Digital Marketing",
    description: "Boost your online presence with our comprehensive digital marketing strategies including SEO, social media management, and online advertising.",
    icon: "cloud",
    slug: "digital-marketing"
  },
  {
    id: 3,
    title: "Mobile App Development",
    description: "Build custom mobile applications for Android and iOS platforms that connect you with your customers wherever they are.",
    icon: "users",
    slug: "app-development"
  },
  {
    id: 4,
    title: "Poster Design",
    description: "Craft eye-catching posters and marketing materials that effectively communicate your message and attract customer attention.",
    icon: "shield",
    slug: "poster-design"
  },
  {
    id: 5,
    title: "UI/UX Design",
    description: "Create intuitive and engaging user interfaces that provide exceptional user experiences and keep customers coming back.",
    icon: "bar-chart",
    slug: "ui-ux-design"
  },
  {
    id: 6,
    title: "Logo & Brand Design",
    description: "Develop a distinctive visual identity with professional logo design and comprehensive branding that communicates your company values.",
    icon: "brain",
    slug: "logo-brand-design"
  }
];
var sampleTeamMembers = [
  {
    id: 1,
    name: "Ananth",
    position: "Founder & CEO",
    bio: "Madurai-based entrepreneur with over 10 years of experience in web development and digital marketing. Passionate about helping local businesses grow through technology.",
    image: "/assets/ceo.jpg",
    linkedIn: "https://linkedin.com",
    twitter: "https://twitter.com"
  },
  {
    id: 2,
    name: "Priya Ramanathan",
    position: "Digital Marketing Lead",
    bio: "Expert in local SEO strategies for Madurai businesses. Has helped numerous companies in Tamil Nadu achieve first-page Google rankings.",
    image: "https://images.unsplash.com/photo-1573496359142-b8d87734a5a2?ixlib=rb-1.2.1&auto=format&fit=crop&w=500&q=80",
    linkedIn: "https://linkedin.com",
    twitter: "https://twitter.com"
  },
  {
    id: 3,
    name: "Raj Sharma",
    position: "Lead Developer",
    bio: "Full-stack developer specializing in responsive websites and mobile applications. Madurai native with deep understanding of local business needs.",
    image: "https://images.unsplash.com/photo-1519085360753-af0119f7cbe7?ixlib=rb-1.2.1&auto=format&fit=crop&w=500&q=80",
    linkedIn: "https://linkedin.com",
    twitter: "https://twitter.com"
  },
  {
    id: 4,
    name: "Lakshmi Venkatesh",
    position: "Creative Director",
    bio: "Award-winning designer with expertise in UI/UX and brand identity. Creates compelling visual experiences for businesses across Tamil Nadu.",
    image: "https://images.unsplash.com/photo-1580894732444-8ecded7900cd?ixlib=rb-1.2.1&auto=format&fit=crop&w=500&q=80",
    linkedIn: "https://linkedin.com",
    twitter: "https://twitter.com"
  }
];
var sampleProjects = [
  {
    id: 1,
    title: "Madurai Silks E-Commerce Store",
    description: "Designed and developed a responsive e-commerce site for a traditional silk saree retailer in Madurai, helping them reach customers across India and internationally.",
    image: "https://images.unsplash.com/photo-1523381294911-8d3cead13475?ixlib=rb-1.2.1&auto=format&fit=crop&w=800&q=80",
    category: "Web Development",
    technologies: ["React", "Node.js", "MongoDB"]
  },
  {
    id: 2,
    title: "Madurai Tourism Mobile App",
    description: "Created an interactive mobile app showcasing Madurai's tourist attractions, local cuisine, and cultural experiences with integrated maps and booking features.",
    image: "https://images.unsplash.com/photo-1604871000636-074fa5117945?ixlib=rb-1.2.1&auto=format&fit=crop&w=800&q=80",
    category: "Mobile App Development",
    technologies: ["React Native", "Express", "Google Maps API"]
  },
  {
    id: 3,
    title: "Sri Meenakshi Hospital Website",
    description: "Developed a modern, patient-friendly website for a leading Madurai hospital with online appointment booking and doctor profiles.",
    image: "https://images.unsplash.com/photo-1519494026892-80bbd2d6fd0d?ixlib=rb-1.2.1&auto=format&fit=crop&w=800&q=80",
    category: "Web Development",
    technologies: ["JavaScript", "PHP", "MySQL"]
  },
  {
    id: 4,
    title: "Madurai Foods Brand Identity",
    description: "Created comprehensive brand identity including logo, packaging, and marketing materials for a local food products company exporting Madurai specialties.",
    image: "https://images.unsplash.com/photo-1583623733237-4d5764a9dc82?ixlib=rb-1.2.1&auto=format&fit=crop&w=800&q=80",
    category: "Logo & Brand Design",
    technologies: ["Adobe Illustrator", "Photoshop", "Packaging Design"]
  },
  {
    id: 5,
    title: "Tamil Nadu Handicrafts Social Media Campaign",
    description: "Managed an integrated social media marketing campaign for a Madurai-based handicrafts cooperative, increasing their online sales by 150% in six months.",
    image: "https://images.unsplash.com/photo-1563986768609-322da13575f3?ixlib=rb-1.2.1&auto=format&fit=crop&w=800&q=80",
    category: "Digital Marketing",
    technologies: ["Facebook Ads", "Instagram", "Content Marketing"]
  },
  {
    id: 6,
    title: "Madurai Real Estate Portal",
    description: "Built a comprehensive property listing and search platform specifically for Madurai's real estate market with virtual tours and neighborhood insights.",
    image: "https://images.unsplash.com/photo-1560518883-ce09059eeffa?ixlib=rb-1.2.1&auto=format&fit=crop&w=800&q=80",
    category: "Web Development",
    technologies: ["React", "Node.js", "PostgreSQL"]
  }
];
var sampleTestimonials = [
  {
    id: 1,
    name: "Venkat Raman",
    position: "Owner",
    company: "Madurai Silks",
    content: "GodivaTech transformed our traditional family business with an e-commerce store that now reaches customers across India and abroad. Our online sales have grown by 85% in just six months. Their understanding of both technology and Madurai's business environment made all the difference.",
    image: "https://randomuser.me/api/portraits/men/45.jpg"
  },
  {
    id: 2,
    name: "Dr. Lakshmi Narayan",
    position: "Director",
    company: "Sri Meenakshi Hospital",
    content: "The website GodivaTech created for our hospital has significantly improved patient experience. The online appointment system has reduced our administrative workload by 40% and patient feedback has been overwhelmingly positive. Their team was professional and understood our unique requirements.",
    image: "https://randomuser.me/api/portraits/women/32.jpg"
  },
  {
    id: 3,
    name: "Rahul Mehta",
    position: "CEO",
    company: "Madurai Properties",
    content: "Our real estate portal developed by GodivaTech has revolutionized how we showcase properties in Madurai. The virtual tour feature and neighborhood insights have received excellent feedback from clients. We've seen a 65% increase in qualified leads since launch.",
    image: "https://randomuser.me/api/portraits/men/28.jpg"
  }
];

// server/storage.ts
import createMemoryStore from "memorystore";
import { eq, desc } from "drizzle-orm";
import session2 from "express-session";

// server/firestore-storage.ts
import session from "express-session";
import MemoryStore from "memorystore";

// server/firebase.ts
import { initializeApp } from "firebase/app";
import { getFirestore } from "firebase/firestore";
import { getAuth } from "firebase/auth";
var firebaseConfig = {
  apiKey: "AIzaSyDzIqWI6AApvWSE22y1Ug7h-8MysAo2fNw",
  authDomain: "godiva-tech.firebaseapp.com",
  projectId: "godiva-tech",
  storageBucket: "godiva-tech.firebasestorage.app",
  messagingSenderId: "801444351245",
  appId: "1:801444351245:web:f030b472d6fb7be3d4f444",
  measurementId: "G-KHE7CZP6EZ"
};
var app = initializeApp(firebaseConfig);
var db = getFirestore(app);
var auth = getAuth(app);

// server/firestore-storage.ts
import {
  collection,
  doc,
  getDoc,
  getDocs,
  query,
  where,
  orderBy,
  limit,
  updateDoc,
  deleteDoc,
  setDoc,
  serverTimestamp,
  Timestamp
} from "firebase/firestore";
var MemoryStoreClass = MemoryStore(session);
var convertTimestampToDate = (timestamp2) => {
  if (timestamp2 instanceof Timestamp) {
    return new Date(timestamp2.toMillis());
  }
  if (timestamp2 && typeof timestamp2.toDate === "function") {
    return timestamp2.toDate();
  }
  return /* @__PURE__ */ new Date();
};
var FirestoreStorage = class {
  // Use 'any' type to avoid SessionStore issues
  constructor() {
    this.sessionStore = new MemoryStoreClass({
      checkPeriod: 864e5
      // prune expired entries every 24h
    });
  }
  // Helper method to get a new ID from a counter
  async getNextId(counterName) {
    const counterRef = doc(db, "counters", counterName);
    const counterSnap = await getDoc(counterRef);
    let nextId = 1;
    if (counterSnap.exists()) {
      nextId = (counterSnap.data()?.count || 0) + 1;
    }
    await setDoc(counterRef, { count: nextId });
    return nextId;
  }
  // User methods
  async getUser(id) {
    try {
      const docRef = doc(db, "users", id.toString());
      const docSnap = await getDoc(docRef);
      if (!docSnap.exists()) return void 0;
      const userData = docSnap.data();
      return { ...userData, id };
    } catch (error) {
      console.error("Error getting user:", error);
      return void 0;
    }
  }
  async getUserByUsername(username) {
    try {
      const usersRef = collection(db, "users");
      const q = query(usersRef, where("username", "==", username), limit(1));
      const querySnapshot = await getDocs(q);
      if (querySnapshot.empty) return void 0;
      const userDoc = querySnapshot.docs[0];
      return { ...userDoc.data(), id: parseInt(userDoc.id) };
    } catch (error) {
      console.error("Error getting user by username:", error);
      return void 0;
    }
  }
  async createUser(insertUser) {
    try {
      const nextId = await this.getNextId("users");
      const userRef = doc(db, "users", nextId.toString());
      const user = { ...insertUser, id: nextId };
      await setDoc(userRef, user);
      return user;
    } catch (error) {
      console.error("Error creating user:", error);
      throw error;
    }
  }
  // Blog posts methods
  async getAllBlogPosts() {
    try {
      const blogPostsRef = collection(db, "blog_posts");
      const q = query(blogPostsRef, orderBy("publishedAt", "desc"));
      const querySnapshot = await getDocs(q);
      return querySnapshot.docs.map((docSnap) => {
        const data = docSnap.data();
        return {
          ...data,
          id: parseInt(docSnap.id),
          publishedAt: convertTimestampToDate(data.publishedAt),
          published: data.published ?? true,
          authorImage: data.authorImage ?? null,
          coverImage: data.coverImage ?? null,
          categoryId: data.categoryId ?? null
        };
      });
    } catch (error) {
      console.error("Error getting all blog posts:", error);
      return [];
    }
  }
  async getBlogPost(id) {
    try {
      const docRef = doc(db, "blog_posts", id.toString());
      const docSnap = await getDoc(docRef);
      if (!docSnap.exists()) return void 0;
      const data = docSnap.data();
      return {
        ...data,
        id,
        publishedAt: convertTimestampToDate(data.publishedAt),
        published: data.published ?? true,
        authorImage: data.authorImage ?? null,
        coverImage: data.coverImage ?? null,
        categoryId: data.categoryId ?? null
      };
    } catch (error) {
      console.error("Error getting blog post:", error);
      return void 0;
    }
  }
  async getBlogPostBySlug(slug) {
    try {
      const blogPostsRef = collection(db, "blog_posts");
      const q = query(blogPostsRef, where("slug", "==", slug), limit(1));
      const querySnapshot = await getDocs(q);
      if (querySnapshot.empty) return void 0;
      const docSnap = querySnapshot.docs[0];
      const data = docSnap.data();
      return {
        ...data,
        id: parseInt(docSnap.id),
        publishedAt: convertTimestampToDate(data.publishedAt),
        published: data.published ?? true,
        authorImage: data.authorImage ?? null,
        coverImage: data.coverImage ?? null,
        categoryId: data.categoryId ?? null
      };
    } catch (error) {
      console.error("Error getting blog post by slug:", error);
      return void 0;
    }
  }
  async createBlogPost(insertBlogPost) {
    try {
      const nextId = await this.getNextId("blog_posts");
      const blogPost = {
        ...insertBlogPost,
        id: nextId,
        published: insertBlogPost.published ?? true,
        authorImage: insertBlogPost.authorImage ?? null,
        coverImage: insertBlogPost.coverImage ?? null,
        publishedAt: insertBlogPost.publishedAt ?? /* @__PURE__ */ new Date(),
        categoryId: insertBlogPost.categoryId ?? null
      };
      const blogPostRef = doc(db, "blog_posts", nextId.toString());
      await setDoc(blogPostRef, {
        ...blogPost,
        publishedAt: serverTimestamp()
        // Use Firestore timestamp for storage
      });
      return blogPost;
    } catch (error) {
      console.error("Error creating blog post:", error);
      throw error;
    }
  }
  async updateBlogPost(id, blogPost) {
    try {
      const blogPostRef = doc(db, "blog_posts", id.toString());
      const docSnap = await getDoc(blogPostRef);
      if (!docSnap.exists()) return void 0;
      await updateDoc(blogPostRef, {
        ...blogPost,
        updatedAt: serverTimestamp()
      });
      const updatedSnap = await getDoc(blogPostRef);
      const data = updatedSnap.data();
      return {
        ...data,
        id,
        publishedAt: convertTimestampToDate(data.publishedAt),
        published: data.published ?? true,
        authorImage: data.authorImage ?? null,
        coverImage: data.coverImage ?? null,
        categoryId: data.categoryId ?? null
      };
    } catch (error) {
      console.error("Error updating blog post:", error);
      return void 0;
    }
  }
  async deleteBlogPost(id) {
    try {
      const docRef = doc(db, "blog_posts", id.toString());
      await deleteDoc(docRef);
      return true;
    } catch (error) {
      console.error("Error deleting blog post:", error);
      return false;
    }
  }
  // Categories methods
  async getAllCategories() {
    try {
      const categoriesRef = collection(db, "categories");
      const q = query(categoriesRef, orderBy("name"));
      const querySnapshot = await getDocs(q);
      return querySnapshot.docs.map((docSnap) => {
        const data = docSnap.data();
        return {
          ...data,
          id: parseInt(docSnap.id)
        };
      });
    } catch (error) {
      console.error("Error getting all categories:", error);
      return [];
    }
  }
  async getCategory(id) {
    try {
      const docRef = doc(db, "categories", id.toString());
      const docSnap = await getDoc(docRef);
      if (!docSnap.exists()) return void 0;
      const data = docSnap.data();
      return { ...data, id };
    } catch (error) {
      console.error("Error getting category:", error);
      return void 0;
    }
  }
  async getCategoryBySlug(slug) {
    try {
      const categoriesRef = collection(db, "categories");
      const q = query(categoriesRef, where("slug", "==", slug), limit(1));
      const querySnapshot = await getDocs(q);
      if (querySnapshot.empty) return void 0;
      const docSnap = querySnapshot.docs[0];
      const data = docSnap.data();
      return { ...data, id: parseInt(docSnap.id) };
    } catch (error) {
      console.error("Error getting category by slug:", error);
      return void 0;
    }
  }
  async createCategory(insertCategory) {
    try {
      const nextId = await this.getNextId("categories");
      const category = {
        ...insertCategory,
        id: nextId
      };
      const categoryRef = doc(db, "categories", nextId.toString());
      await setDoc(categoryRef, category);
      return category;
    } catch (error) {
      console.error("Error creating category:", error);
      throw error;
    }
  }
  async updateCategory(id, category) {
    try {
      const categoryRef = doc(db, "categories", id.toString());
      const docSnap = await getDoc(categoryRef);
      if (!docSnap.exists()) return void 0;
      await updateDoc(categoryRef, {
        ...category,
        updatedAt: serverTimestamp()
      });
      const updatedSnap = await getDoc(categoryRef);
      const data = updatedSnap.data();
      return { ...data, id };
    } catch (error) {
      console.error("Error updating category:", error);
      return void 0;
    }
  }
  async deleteCategory(id) {
    try {
      const docRef = doc(db, "categories", id.toString());
      await deleteDoc(docRef);
      return true;
    } catch (error) {
      console.error("Error deleting category:", error);
      return false;
    }
  }
  // Projects methods
  async getAllProjects() {
    try {
      const projectsRef = collection(db, "projects");
      const q = query(projectsRef, orderBy("title"));
      const querySnapshot = await getDocs(q);
      console.log(`Found ${querySnapshot.size} projects in Firestore collection`);
      return querySnapshot.docs.map((docSnap) => {
        const data = docSnap.data();
        const projectId = docSnap.id;
        console.log(`Processing project: id=${projectId}, title=${data.title}`);
        return {
          ...data,
          id: projectId,
          // Use the Firebase document ID directly
          docId: projectId,
          // Also store the document ID in docId for reference
          firebaseId: projectId,
          // Additional field to make it clear this is a Firebase ID
          link: data.link ?? null,
          image: data.image ?? null
        };
      });
    } catch (error) {
      console.error("Error getting all projects:", error);
      return [];
    }
  }
  async getProject(id) {
    try {
      const docId = id.toString();
      console.log(`Fetching project with document ID: ${docId}`);
      const docRef = doc(db, "projects", docId);
      const docSnap = await getDoc(docRef);
      if (!docSnap.exists()) {
        console.log(`No project document found with ID: ${docId}`);
        if (!isNaN(Number(id))) {
          console.log(`Trying to find project by numeric ID: ${id}`);
          const allProjects = await this.getAllProjects();
          const projectWithNumericId = allProjects.find((p) => {
            const numericId = p.id && typeof p.id === "string" ? parseInt(p.id) : null;
            return numericId === Number(id);
          });
          if (projectWithNumericId) {
            console.log(`Found project with numeric ID match: ${projectWithNumericId.id}, title: ${projectWithNumericId.title}`);
            return projectWithNumericId;
          }
        }
        return void 0;
      }
      const data = docSnap.data();
      console.log(`Found project with document ID: ${docId}, title: ${data.title}`);
      return {
        ...data,
        id: docId,
        // Use the document ID as is
        docId,
        // Also include the doc ID separately
        firebaseId: docId,
        // Explicit field indicating this is a Firebase ID
        link: data.link ?? null,
        image: data.image ?? null
      };
    } catch (error) {
      console.error(`Error getting project with ID ${id}:`, error);
      return void 0;
    }
  }
  async createProject(insertProject) {
    try {
      console.log("Creating new project in Firestore with data:", insertProject);
      const projectsCollection = collection(db, "projects");
      const newDocRef = doc(projectsCollection);
      const newDocId = newDocRef.id;
      console.log(`Generated new Firestore document ID: ${newDocId}`);
      const projectData = {
        ...insertProject,
        // Include these properties but not in the 'id' field which is handled specially
        link: insertProject.link ?? null,
        image: insertProject.image ?? null,
        gallery: insertProject.gallery ?? null,
        fullDescription: insertProject.fullDescription ?? null,
        clientName: insertProject.clientName ?? null,
        completionDate: insertProject.completionDate ?? null,
        githubLink: insertProject.githubLink ?? null,
        testimonial: insertProject.testimonial ?? null,
        challengeDescription: insertProject.challengeDescription ?? null,
        solutionDescription: insertProject.solutionDescription ?? null,
        resultsDescription: insertProject.resultsDescription ?? null,
        featured: insertProject.featured ?? false,
        order: insertProject.order ?? null,
        createdAt: serverTimestamp(),
        updatedAt: serverTimestamp()
      };
      await setDoc(newDocRef, projectData);
      console.log(`Successfully created project with ID: ${newDocId}`);
      const project = {
        ...projectData,
        id: newDocId,
        // Use the Firebase document ID
        docId: newDocId,
        // Also include document ID explicitly 
        firebaseId: newDocId
        // Additional field to be explicit
      };
      return project;
    } catch (error) {
      console.error("Error creating project:", error);
      throw error;
    }
  }
  async updateProject(id, project) {
    try {
      console.log(`=== FIRESTORE PROJECT UPDATE ===`);
      console.log(`Attempting to update project with ID: ${id} (type: ${typeof id}) in Firestore`);
      const docId = id.toString();
      const projectRef = doc(db, "projects", docId);
      const docSnap = await getDoc(projectRef);
      if (!docSnap.exists()) {
        console.warn(`Project with direct document ID ${docId} not found in Firestore, searching all projects...`);
        const projectsCollection = collection(db, "projects");
        const querySnapshot = await getDocs(projectsCollection);
        console.log(`Found ${querySnapshot.size} projects in Firestore collection to search through`);
        let foundDoc = null;
        querySnapshot.forEach((docSnapshot) => {
          const data3 = docSnapshot.data();
          console.log(`Checking project: id=${docSnapshot.id}, title=${data3.title || "unknown"}`);
          if (docSnapshot.id === docId || // Direct document ID match
          data3.id && String(data3.id) === String(id) || // ID property match
          data3.id && typeof data3.id === "number" && typeof id === "string" && !isNaN(Number(id)) && data3.id === Number(id)) {
            console.log(`\u2713 Found matching project: id=${docSnapshot.id}, title=${data3.title || "unknown"}`);
            foundDoc = docSnapshot;
          }
        });
        if (!foundDoc) {
          console.warn(`No matching project found for ID: ${id} in any format`);
          return void 0;
        }
        console.log(`Using found document ID: ${foundDoc.id} for update operation`);
        const foundDocRef = doc(db, "projects", foundDoc.id);
        await updateDoc(foundDocRef, {
          ...project,
          updatedAt: serverTimestamp()
        });
        const updatedSnap2 = await getDoc(foundDocRef);
        const data2 = updatedSnap2.data();
        console.log(`Successfully updated project with document ID: ${foundDoc.id} in Firestore`);
        return {
          ...data2,
          id,
          // Keep the original ID
          docId: foundDoc.id,
          // Include the Firestore document ID
          firebaseId: foundDoc.id,
          // For compatibility
          link: data2.link ?? null,
          image: data2.image ?? null
        };
      }
      console.log(`Project found using direct document ID, updating with new data`, project);
      await updateDoc(projectRef, {
        ...project,
        updatedAt: serverTimestamp()
      });
      const updatedSnap = await getDoc(projectRef);
      const data = updatedSnap.data();
      console.log(`Successfully updated project with document ID: ${docId} in Firestore`);
      return {
        ...data,
        id,
        // Preserve the original ID that was passed in
        docId,
        // Include the Firestore document ID
        firebaseId: docId,
        // For compatibility
        link: data.link ?? null,
        image: data.image ?? null
      };
    } catch (error) {
      console.error(`Error updating project with ID ${id} in Firestore:`, error);
      return void 0;
    }
  }
  async deleteProject(id) {
    try {
      console.log(`Attempting to delete project with ID: ${id} (type: ${typeof id})`);
      if (id === null || id === void 0) {
        console.error(`Invalid project ID for deletion: ${id}`);
        return false;
      }
      console.log("Retrieving all projects to find the target document");
      const projectsCollection = collection(db, "projects");
      const querySnapshot = await getDocs(projectsCollection);
      console.log(`Found ${querySnapshot.size} projects in collection`);
      const targetDocId = typeof id === "string" ? id : id.toString();
      let foundDoc = null;
      querySnapshot.forEach((docSnapshot) => {
        const data = docSnapshot.data();
        console.log(`Checking project: id=${docSnapshot.id}, title=${data.title || "unknown"}`);
        if (docSnapshot.id === targetDocId || data.id && (typeof data.id === "number" && data.id === Number(id) || typeof data.id === "string" && data.id === String(id) || String(data.id) === targetDocId)) {
          console.log(`\u2713 Found matching project: id=${docSnapshot.id}, title=${data.title || "unknown"}`);
          foundDoc = docSnapshot;
        }
      });
      if (!foundDoc) {
        console.warn(`No matching project found for ID: ${id}`);
        return false;
      }
      console.log(`Project found with document ID: ${foundDoc.id}`);
      console.log(`Proceeding with deletion of document 'projects/${foundDoc.id}'`);
      try {
        const projectRef = doc(db, "projects", foundDoc.id);
        await deleteDoc(projectRef);
        const verifySnap = await getDoc(projectRef);
        if (!verifySnap.exists()) {
          console.log(`Project with document ID ${foundDoc.id} successfully deleted!`);
          return true;
        } else {
          console.error(`Deletion operation did not remove document 'projects/${foundDoc.id}'`);
          return false;
        }
      } catch (deleteError) {
        console.error(`Error during document deletion:`, deleteError);
        return false;
      }
    } catch (error) {
      console.error(`Error in deleteProject with ID ${id}:`, error);
      return false;
    }
  }
  // Services methods
  async getAllServices() {
    try {
      const servicesRef = collection(db, "services");
      const q = query(servicesRef, orderBy("title"));
      const querySnapshot = await getDocs(q);
      return querySnapshot.docs.map((docSnap) => {
        const data = docSnap.data();
        return { ...data, id: parseInt(docSnap.id) };
      });
    } catch (error) {
      console.error("Error getting all services:", error);
      return [];
    }
  }
  async getService(id) {
    try {
      const docRef = doc(db, "services", id.toString());
      const docSnap = await getDoc(docRef);
      if (!docSnap.exists()) return void 0;
      const data = docSnap.data();
      return { ...data, id };
    } catch (error) {
      console.error("Error getting service:", error);
      return void 0;
    }
  }
  async getServiceBySlug(slug) {
    try {
      const servicesRef = collection(db, "services");
      const q = query(servicesRef, where("slug", "==", slug), limit(1));
      const querySnapshot = await getDocs(q);
      if (querySnapshot.empty) return void 0;
      const docSnap = querySnapshot.docs[0];
      const data = docSnap.data();
      return { ...data, id: parseInt(docSnap.id) };
    } catch (error) {
      console.error("Error getting service by slug:", error);
      return void 0;
    }
  }
  async createService(insertService) {
    try {
      const nextId = await this.getNextId("services");
      const service = {
        ...insertService,
        id: nextId
      };
      const serviceRef = doc(db, "services", nextId.toString());
      await setDoc(serviceRef, service);
      return service;
    } catch (error) {
      console.error("Error creating service:", error);
      throw error;
    }
  }
  async updateService(id, service) {
    try {
      const serviceRef = doc(db, "services", id.toString());
      const docSnap = await getDoc(serviceRef);
      if (!docSnap.exists()) return void 0;
      await updateDoc(serviceRef, {
        ...service,
        updatedAt: serverTimestamp()
      });
      const updatedSnap = await getDoc(serviceRef);
      const data = updatedSnap.data();
      return { ...data, id };
    } catch (error) {
      console.error("Error updating service:", error);
      return void 0;
    }
  }
  async deleteService(id) {
    try {
      const docRef = doc(db, "services", id.toString());
      await deleteDoc(docRef);
      return true;
    } catch (error) {
      console.error("Error deleting service:", error);
      return false;
    }
  }
  // Team members methods
  async getAllTeamMembers() {
    try {
      const teamMembersRef = collection(db, "team_members");
      const q = query(teamMembersRef, orderBy("name"));
      const querySnapshot = await getDocs(q);
      return querySnapshot.docs.map((docSnap) => {
        const data = docSnap.data();
        return {
          ...data,
          id: parseInt(docSnap.id),
          image: data.image ?? null,
          linkedIn: data.linkedIn ?? null,
          twitter: data.twitter ?? null
        };
      });
    } catch (error) {
      console.error("Error getting all team members:", error);
      return [];
    }
  }
  async getTeamMember(id) {
    try {
      const docRef = doc(db, "team_members", id.toString());
      const docSnap = await getDoc(docRef);
      if (!docSnap.exists()) return void 0;
      const data = docSnap.data();
      return {
        ...data,
        id,
        image: data.image ?? null,
        linkedIn: data.linkedIn ?? null,
        twitter: data.twitter ?? null
      };
    } catch (error) {
      console.error("Error getting team member:", error);
      return void 0;
    }
  }
  async createTeamMember(insertTeamMember) {
    try {
      const nextId = await this.getNextId("team_members");
      const teamMember = {
        ...insertTeamMember,
        id: nextId,
        image: insertTeamMember.image ?? null,
        linkedIn: insertTeamMember.linkedIn ?? null,
        twitter: insertTeamMember.twitter ?? null
      };
      const teamMemberRef = doc(db, "team_members", nextId.toString());
      await setDoc(teamMemberRef, teamMember);
      return teamMember;
    } catch (error) {
      console.error("Error creating team member:", error);
      throw error;
    }
  }
  async updateTeamMember(id, teamMember) {
    try {
      const teamMemberRef = doc(db, "team_members", id.toString());
      const docSnap = await getDoc(teamMemberRef);
      if (!docSnap.exists()) return void 0;
      await updateDoc(teamMemberRef, {
        ...teamMember,
        updatedAt: serverTimestamp()
      });
      const updatedSnap = await getDoc(teamMemberRef);
      const data = updatedSnap.data();
      return {
        ...data,
        id,
        image: data.image ?? null,
        linkedIn: data.linkedIn ?? null,
        twitter: data.twitter ?? null
      };
    } catch (error) {
      console.error("Error updating team member:", error);
      return void 0;
    }
  }
  async deleteTeamMember(id) {
    try {
      const docRef = doc(db, "team_members", id.toString());
      await deleteDoc(docRef);
      return true;
    } catch (error) {
      console.error("Error deleting team member:", error);
      return false;
    }
  }
  // Testimonials methods
  async getAllTestimonials() {
    try {
      const testimonialsRef = collection(db, "testimonials");
      const q = query(testimonialsRef, orderBy("name"));
      const querySnapshot = await getDocs(q);
      return querySnapshot.docs.map((docSnap) => {
        const data = docSnap.data();
        return {
          ...data,
          id: parseInt(docSnap.id),
          image: data.image ?? null
        };
      });
    } catch (error) {
      console.error("Error getting all testimonials:", error);
      return [];
    }
  }
  async getTestimonial(id) {
    try {
      const docRef = doc(db, "testimonials", id.toString());
      const docSnap = await getDoc(docRef);
      if (!docSnap.exists()) return void 0;
      const data = docSnap.data();
      return {
        ...data,
        id,
        image: data.image ?? null
      };
    } catch (error) {
      console.error("Error getting testimonial:", error);
      return void 0;
    }
  }
  async createTestimonial(insertTestimonial) {
    try {
      const nextId = await this.getNextId("testimonials");
      const testimonial = {
        ...insertTestimonial,
        id: nextId,
        image: insertTestimonial.image ?? null
      };
      const testimonialRef = doc(db, "testimonials", nextId.toString());
      await setDoc(testimonialRef, testimonial);
      return testimonial;
    } catch (error) {
      console.error("Error creating testimonial:", error);
      throw error;
    }
  }
  async updateTestimonial(id, testimonial) {
    try {
      const testimonialRef = doc(db, "testimonials", id.toString());
      const docSnap = await getDoc(testimonialRef);
      if (!docSnap.exists()) return void 0;
      await updateDoc(testimonialRef, {
        ...testimonial,
        updatedAt: serverTimestamp()
      });
      const updatedSnap = await getDoc(testimonialRef);
      const data = updatedSnap.data();
      return {
        ...data,
        id,
        image: data.image ?? null
      };
    } catch (error) {
      console.error("Error updating testimonial:", error);
      return void 0;
    }
  }
  async deleteTestimonial(id) {
    try {
      const docRef = doc(db, "testimonials", id.toString());
      await deleteDoc(docRef);
      return true;
    } catch (error) {
      console.error("Error deleting testimonial:", error);
      return false;
    }
  }
  // Contact messages methods
  async getAllContactMessages() {
    try {
      const contactMessagesRef = collection(db, "contact_messages");
      const q = query(contactMessagesRef, orderBy("createdAt", "desc"));
      const querySnapshot = await getDocs(q);
      return querySnapshot.docs.map((docSnap) => {
        const data = docSnap.data();
        return {
          ...data,
          id: parseInt(docSnap.id),
          createdAt: convertTimestampToDate(data.createdAt)
        };
      });
    } catch (error) {
      console.error("Error getting all contact messages:", error);
      return [];
    }
  }
  async getContactMessage(id) {
    try {
      const docRef = doc(db, "contact_messages", id.toString());
      const docSnap = await getDoc(docRef);
      if (!docSnap.exists()) return void 0;
      const data = docSnap.data();
      return {
        ...data,
        id,
        createdAt: convertTimestampToDate(data.createdAt)
      };
    } catch (error) {
      console.error("Error getting contact message:", error);
      return void 0;
    }
  }
  async createContactMessage(insertContactMessage) {
    try {
      const nextId = await this.getNextId("contact_messages");
      const contactMessage = {
        ...insertContactMessage,
        id: nextId,
        phone: insertContactMessage.phone ?? null,
        createdAt: /* @__PURE__ */ new Date()
      };
      const contactMessageRef = doc(db, "contact_messages", nextId.toString());
      await setDoc(contactMessageRef, {
        ...contactMessage,
        createdAt: serverTimestamp()
        // Use Firestore timestamp for storage
      });
      return contactMessage;
    } catch (error) {
      console.error("Error creating contact message:", error);
      throw error;
    }
  }
  async deleteContactMessage(id) {
    try {
      const docRef = doc(db, "contact_messages", id.toString());
      await deleteDoc(docRef);
      return true;
    } catch (error) {
      console.error("Error deleting contact message:", error);
      return false;
    }
  }
  // Subscribers methods
  async getAllSubscribers() {
    try {
      const subscribersRef = collection(db, "subscribers");
      const q = query(subscribersRef, orderBy("createdAt", "desc"));
      const querySnapshot = await getDocs(q);
      return querySnapshot.docs.map((docSnap) => {
        const data = docSnap.data();
        return {
          ...data,
          id: parseInt(docSnap.id),
          createdAt: convertTimestampToDate(data.createdAt)
        };
      });
    } catch (error) {
      console.error("Error getting all subscribers:", error);
      return [];
    }
  }
  async getSubscriber(id) {
    try {
      const docRef = doc(db, "subscribers", id.toString());
      const docSnap = await getDoc(docRef);
      if (!docSnap.exists()) return void 0;
      const data = docSnap.data();
      return {
        ...data,
        id,
        createdAt: convertTimestampToDate(data.createdAt)
      };
    } catch (error) {
      console.error("Error getting subscriber:", error);
      return void 0;
    }
  }
  async getSubscriberByEmail(email) {
    try {
      const subscribersRef = collection(db, "subscribers");
      const q = query(subscribersRef, where("email", "==", email), limit(1));
      const querySnapshot = await getDocs(q);
      if (querySnapshot.empty) return void 0;
      const docSnap = querySnapshot.docs[0];
      const data = docSnap.data();
      return {
        ...data,
        id: parseInt(docSnap.id),
        createdAt: convertTimestampToDate(data.createdAt)
      };
    } catch (error) {
      console.error("Error getting subscriber by email:", error);
      return void 0;
    }
  }
  async createSubscriber(insertSubscriber) {
    try {
      const nextId = await this.getNextId("subscribers");
      const subscriber = {
        ...insertSubscriber,
        id: nextId,
        createdAt: /* @__PURE__ */ new Date()
      };
      const subscriberRef = doc(db, "subscribers", nextId.toString());
      await setDoc(subscriberRef, {
        ...subscriber,
        createdAt: serverTimestamp()
        // Use Firestore timestamp for storage
      });
      return subscriber;
    } catch (error) {
      console.error("Error creating subscriber:", error);
      throw error;
    }
  }
  async deleteSubscriber(id) {
    try {
      const docRef = doc(db, "subscribers", id.toString());
      await deleteDoc(docRef);
      return true;
    } catch (error) {
      console.error("Error deleting subscriber:", error);
      return false;
    }
  }
};
var firestoreStorage = new FirestoreStorage();

// server/storage.ts
var MemStorage = class {
  constructor() {
    this.users = /* @__PURE__ */ new Map();
    this.blogPosts = /* @__PURE__ */ new Map();
    this.categories = /* @__PURE__ */ new Map();
    this.projects = /* @__PURE__ */ new Map();
    this.services = /* @__PURE__ */ new Map();
    this.teamMembers = /* @__PURE__ */ new Map();
    this.testimonials = /* @__PURE__ */ new Map();
    this.contactMessages = /* @__PURE__ */ new Map();
    this.subscribers = /* @__PURE__ */ new Map();
    this.userCurrentId = 1;
    this.blogPostCurrentId = 1;
    this.categoryCurrentId = 1;
    this.projectCurrentId = 1;
    this.serviceCurrentId = 1;
    this.teamMemberCurrentId = 1;
    this.testimonialCurrentId = 1;
    this.contactMessageCurrentId = 1;
    this.subscriberCurrentId = 1;
    this.initializeSampleData();
  }
  initializeSampleData() {
    sampleCategories.forEach((category) => {
      this.categories.set(category.id, { ...category });
      if (category.id >= this.categoryCurrentId) {
        this.categoryCurrentId = category.id + 1;
      }
    });
    sampleBlogPosts.forEach((post) => {
      this.blogPosts.set(post.id, { ...post });
      if (post.id >= this.blogPostCurrentId) {
        this.blogPostCurrentId = post.id + 1;
      }
    });
    sampleServices.forEach((service) => {
      this.services.set(service.id, { ...service });
      if (service.id >= this.serviceCurrentId) {
        this.serviceCurrentId = service.id + 1;
      }
    });
    sampleTeamMembers.forEach((member) => {
      this.teamMembers.set(member.id, { ...member });
      if (member.id >= this.teamMemberCurrentId) {
        this.teamMemberCurrentId = member.id + 1;
      }
    });
    sampleProjects.forEach((project) => {
      this.projects.set(project.id, { ...project });
      if (project.id >= this.projectCurrentId) {
        this.projectCurrentId = project.id + 1;
      }
    });
    sampleTestimonials.forEach((testimonial) => {
      this.testimonials.set(testimonial.id, { ...testimonial });
      if (testimonial.id >= this.testimonialCurrentId) {
        this.testimonialCurrentId = testimonial.id + 1;
      }
    });
  }
  // User methods
  async getUser(id) {
    return this.users.get(id);
  }
  async getUserByUsername(username) {
    return Array.from(this.users.values()).find(
      (user) => user.username === username
    );
  }
  async createUser(insertUser) {
    const id = this.userCurrentId++;
    const user = { ...insertUser, id };
    this.users.set(id, user);
    return user;
  }
  // Blog posts methods
  async getAllBlogPosts() {
    return Array.from(this.blogPosts.values()).sort((a, b) => {
      return new Date(b.publishedAt).getTime() - new Date(a.publishedAt).getTime();
    });
  }
  async getBlogPost(id) {
    return this.blogPosts.get(id);
  }
  async getBlogPostBySlug(slug) {
    return Array.from(this.blogPosts.values()).find(
      (post) => post.slug === slug
    );
  }
  async createBlogPost(insertBlogPost) {
    const id = this.blogPostCurrentId++;
    const blogPost = { ...insertBlogPost, id };
    this.blogPosts.set(id, blogPost);
    return blogPost;
  }
  // Categories methods
  async getAllCategories() {
    return Array.from(this.categories.values());
  }
  async getCategory(id) {
    return this.categories.get(id);
  }
  async getCategoryBySlug(slug) {
    return Array.from(this.categories.values()).find(
      (category) => category.slug === slug
    );
  }
  async createCategory(insertCategory) {
    const id = this.categoryCurrentId++;
    const category = { ...insertCategory, id };
    this.categories.set(id, category);
    return category;
  }
  // Projects methods
  async getAllProjects() {
    return Array.from(this.projects.values());
  }
  async getProject(id) {
    return this.projects.get(id);
  }
  async createProject(insertProject) {
    const id = this.projectCurrentId++;
    const project = { ...insertProject, id };
    this.projects.set(id, project);
    return project;
  }
  // Services methods
  async getAllServices() {
    return Array.from(this.services.values());
  }
  async getService(id) {
    return this.services.get(id);
  }
  async getServiceBySlug(slug) {
    return Array.from(this.services.values()).find(
      (service) => service.slug === slug
    );
  }
  async createService(insertService) {
    const id = this.serviceCurrentId++;
    const service = { ...insertService, id };
    this.services.set(id, service);
    return service;
  }
  // Team members methods
  async getAllTeamMembers() {
    return Array.from(this.teamMembers.values());
  }
  async getTeamMember(id) {
    return this.teamMembers.get(id);
  }
  async createTeamMember(insertTeamMember) {
    const id = this.teamMemberCurrentId++;
    const teamMember = { ...insertTeamMember, id };
    this.teamMembers.set(id, teamMember);
    return teamMember;
  }
  // Testimonials methods
  async getAllTestimonials() {
    return Array.from(this.testimonials.values());
  }
  async getTestimonial(id) {
    return this.testimonials.get(id);
  }
  async createTestimonial(insertTestimonial) {
    const id = this.testimonialCurrentId++;
    const testimonial = { ...insertTestimonial, id };
    this.testimonials.set(id, testimonial);
    return testimonial;
  }
  // Contact messages methods
  async getAllContactMessages() {
    return Array.from(this.contactMessages.values());
  }
  async getContactMessage(id) {
    return this.contactMessages.get(id);
  }
  async createContactMessage(insertContactMessage) {
    const id = this.contactMessageCurrentId++;
    const createdAt = /* @__PURE__ */ new Date();
    const contactMessage = { ...insertContactMessage, id, createdAt };
    this.contactMessages.set(id, contactMessage);
    return contactMessage;
  }
  // Subscribers methods
  async getAllSubscribers() {
    return Array.from(this.subscribers.values());
  }
  async getSubscriber(id) {
    return this.subscribers.get(id);
  }
  async getSubscriberByEmail(email) {
    return Array.from(this.subscribers.values()).find(
      (subscriber) => subscriber.email === email
    );
  }
  async createSubscriber(insertSubscriber) {
    const id = this.subscriberCurrentId++;
    const createdAt = /* @__PURE__ */ new Date();
    const subscriber = { ...insertSubscriber, id, createdAt };
    this.subscribers.set(id, subscriber);
    return subscriber;
  }
};
MemStorage.prototype.updateBlogPost = async function(id, blogPost) {
  const existingPost = this.blogPosts.get(id);
  if (!existingPost) {
    return void 0;
  }
  const updatedPost = { ...existingPost, ...blogPost };
  this.blogPosts.set(id, updatedPost);
  return updatedPost;
};
MemStorage.prototype.deleteBlogPost = async function(id) {
  return this.blogPosts.delete(id);
};
MemStorage.prototype.updateCategory = async function(id, category) {
  const existingCategory = this.categories.get(id);
  if (!existingCategory) {
    return void 0;
  }
  const updatedCategory = { ...existingCategory, ...category };
  this.categories.set(id, updatedCategory);
  return updatedCategory;
};
MemStorage.prototype.deleteCategory = async function(id) {
  return this.categories.delete(id);
};
MemStorage.prototype.updateProject = async function(id, project) {
  let projectId = id;
  if (typeof id === "string" && !isNaN(Number(id))) {
    projectId = Number(id);
  }
  const existingProject = this.projects.get(projectId);
  if (!existingProject) {
    if (typeof id === "string" && typeof projectId === "number") {
      const stringIdProject = this.projects.get(id);
      if (stringIdProject) {
        const updatedProject2 = { ...stringIdProject, ...project };
        this.projects.set(id, updatedProject2);
        return updatedProject2;
      }
    }
    return void 0;
  }
  const updatedProject = { ...existingProject, ...project };
  this.projects.set(projectId, updatedProject);
  return updatedProject;
};
MemStorage.prototype.deleteProject = async function(id) {
  if (typeof id === "string" && !isNaN(Number(id))) {
    return this.projects.delete(Number(id));
  }
  return this.projects.delete(typeof id === "string" ? id : Number(id));
};
MemStorage.prototype.updateService = async function(id, service) {
  const existingService = this.services.get(id);
  if (!existingService) {
    return void 0;
  }
  const updatedService = { ...existingService, ...service };
  this.services.set(id, updatedService);
  return updatedService;
};
MemStorage.prototype.deleteService = async function(id) {
  return this.services.delete(id);
};
MemStorage.prototype.updateTeamMember = async function(id, teamMember) {
  const existingMember = this.teamMembers.get(id);
  if (!existingMember) {
    return void 0;
  }
  const updatedMember = { ...existingMember, ...teamMember };
  this.teamMembers.set(id, updatedMember);
  return updatedMember;
};
MemStorage.prototype.deleteTeamMember = async function(id) {
  return this.teamMembers.delete(id);
};
MemStorage.prototype.updateTestimonial = async function(id, testimonial) {
  const existingTestimonial = this.testimonials.get(id);
  if (!existingTestimonial) {
    return void 0;
  }
  const updatedTestimonial = { ...existingTestimonial, ...testimonial };
  this.testimonials.set(id, updatedTestimonial);
  return updatedTestimonial;
};
MemStorage.prototype.deleteTestimonial = async function(id) {
  return this.testimonials.delete(id);
};
MemStorage.prototype.deleteContactMessage = async function(id) {
  return this.contactMessages.delete(id);
};
MemStorage.prototype.deleteSubscriber = async function(id) {
  return this.subscribers.delete(id);
};
var MemoryStore2 = createMemoryStore(session2);
Object.defineProperty(MemStorage.prototype, "sessionStore", {
  get: function() {
    if (!this._sessionStore) {
      this._sessionStore = new MemoryStore2({
        checkPeriod: 864e5
        // prune expired entries every 24h
      });
    }
    return this._sessionStore;
  }
});
var storage = firestoreStorage;

// server/routes.ts
import { z } from "zod";

// server/auth.ts
import passport from "passport";
import { Strategy as LocalStrategy } from "passport-local";
import session3 from "express-session";
import { scrypt, randomBytes, timingSafeEqual } from "crypto";
import { promisify } from "util";
var scryptAsync = promisify(scrypt);
async function hashPassword(password) {
  const salt = randomBytes(16).toString("hex");
  const buf = await scryptAsync(password, salt, 64);
  return `${buf.toString("hex")}.${salt}`;
}
async function comparePasswords(supplied, stored) {
  const [hashed, salt] = stored.split(".");
  const hashedBuf = Buffer.from(hashed, "hex");
  const suppliedBuf = await scryptAsync(supplied, salt, 64);
  return timingSafeEqual(hashedBuf, suppliedBuf);
}
function setupAuth(app3) {
  if (!process.env.SESSION_SECRET) {
    process.env.SESSION_SECRET = randomBytes(32).toString("hex");
  }
  const sessionSettings = {
    secret: process.env.SESSION_SECRET,
    resave: false,
    saveUninitialized: false,
    store: storage.sessionStore,
    cookie: {
      secure: process.env.NODE_ENV === "production",
      maxAge: 14 * 24 * 60 * 60 * 1e3,
      // 14 days for persistent sessions
      httpOnly: true,
      sameSite: "lax"
    },
    rolling: true
    // Reset expiration on every response
  };
  app3.set("trust proxy", 1);
  app3.use(session3(sessionSettings));
  app3.use(passport.initialize());
  app3.use(passport.session());
  passport.use(
    new LocalStrategy(async (username, password, done) => {
      const user = await storage.getUserByUsername(username);
      if (!user || !await comparePasswords(password, user.password)) {
        return done(null, false);
      } else {
        return done(null, user);
      }
    })
  );
  passport.serializeUser((user, done) => done(null, user.id));
  passport.deserializeUser(async (id, done) => {
    const user = await storage.getUser(id);
    done(null, user);
  });
  app3.post("/api/register", async (req, res, next) => {
    const existingUser = await storage.getUserByUsername(req.body.username);
    if (existingUser) {
      return res.status(400).send("Username already exists");
    }
    const user = await storage.createUser({
      ...req.body,
      password: await hashPassword(req.body.password)
    });
    req.login(user, (err) => {
      if (err) return next(err);
      res.status(201).json(user);
    });
  });
  app3.post("/api/login", passport.authenticate("local"), (req, res) => {
    const rememberMe = req.body.rememberMe === true;
    if (rememberMe && req.session.cookie) {
      req.session.cookie.maxAge = 30 * 24 * 60 * 60 * 1e3;
      console.log("Remember Me enabled, extending session to 30 days");
    } else if (req.session.cookie) {
      req.session.cookie.maxAge = 2 * 60 * 60 * 1e3;
      console.log("Standard session length of 2 hours set");
    }
    req.session.save((err) => {
      if (err) {
        console.error("Error saving session:", err);
      }
      res.status(200).json(req.user);
    });
  });
  app3.post("/api/logout", (req, res, next) => {
    req.logout((err) => {
      if (err) return next(err);
      res.sendStatus(200);
    });
  });
  app3.get("/api/user", (req, res) => {
    if (!req.isAuthenticated()) return res.sendStatus(401);
    res.json(req.user);
  });
  app3.use("/api/admin/*", (req, res, next) => {
    if (!req.isAuthenticated()) {
      return res.status(401).json({ message: "Not authenticated" });
    }
    next();
  });
}
function isAuthenticated(req, res, next) {
  if (req.isAuthenticated()) {
    return next();
  }
  res.status(401).json({ message: "Not authenticated" });
}

// server/cloudinary.ts
import { v2 as cloudinary } from "cloudinary";

// server/vite.ts
import express from "express";
import fs from "fs";
import path2 from "path";
import { createServer as createViteServer, createLogger } from "vite";

// vite.config.ts
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
import themePlugin from "@replit/vite-plugin-shadcn-theme-json";
import path from "path";
import runtimeErrorOverlay from "@replit/vite-plugin-runtime-error-modal";
var vite_config_default = defineConfig({
  plugins: [
    react(),
    runtimeErrorOverlay(),
    themePlugin(),
    ...process.env.NODE_ENV !== "production" && process.env.REPL_ID !== void 0 ? [
      await import("@replit/vite-plugin-cartographer").then(
        (m) => m.cartographer()
      )
    ] : []
  ],
  resolve: {
    alias: {
      "@": path.resolve(import.meta.dirname, "client", "src"),
      "@shared": path.resolve(import.meta.dirname, "shared"),
      "@assets": path.resolve(import.meta.dirname, "attached_assets")
    }
  },
  root: path.resolve(import.meta.dirname, "client"),
  build: {
    outDir: path.resolve(import.meta.dirname, "dist/public"),
    emptyOutDir: true
  }
});

// server/vite.ts
import { nanoid } from "nanoid";
var viteLogger = createLogger();
function log(message, source = "express") {
  const formattedTime = (/* @__PURE__ */ new Date()).toLocaleTimeString("en-US", {
    hour: "numeric",
    minute: "2-digit",
    second: "2-digit",
    hour12: true
  });
  console.log(`${formattedTime} [${source}] ${message}`);
}
async function setupVite(app3, server) {
  const serverOptions = {
    middlewareMode: true,
    hmr: { server },
    allowedHosts: true
  };
  const vite = await createViteServer({
    ...vite_config_default,
    configFile: false,
    customLogger: {
      ...viteLogger,
      error: (msg, options) => {
        viteLogger.error(msg, options);
        process.exit(1);
      }
    },
    server: serverOptions,
    appType: "custom"
  });
  app3.use(vite.middlewares);
  app3.use("*", async (req, res, next) => {
    const url = req.originalUrl;
    try {
      const clientTemplate = path2.resolve(
        import.meta.dirname,
        "..",
        "client",
        "index.html"
      );
      let template = await fs.promises.readFile(clientTemplate, "utf-8");
      template = template.replace(
        `src="/src/main.tsx"`,
        `src="/src/main.tsx?v=${nanoid()}"`
      );
      const page = await vite.transformIndexHtml(url, template);
      res.status(200).set({ "Content-Type": "text/html" }).end(page);
    } catch (e) {
      vite.ssrFixStacktrace(e);
      next(e);
    }
  });
}
function serveStatic(app3) {
  const distPath = path2.resolve(import.meta.dirname, "public");
  if (!fs.existsSync(distPath)) {
    throw new Error(
      `Could not find the build directory: ${distPath}, make sure to build the client first`
    );
  }
  app3.use(express.static(distPath));
  app3.use("*", (_req, res) => {
    res.sendFile(path2.resolve(distPath, "index.html"));
  });
}

// server/cloudinary.ts
var CLOUDINARY_CLOUD_NAME = "doeodacsg";
var CLOUDINARY_API_KEY = "269267633995791";
var CLOUDINARY_API_SECRET = "wUw9Seu6drQEIbQ1tAvYeVyqHdU";
var getCloudinaryConfig = () => {
  return {
    cloudName: CLOUDINARY_CLOUD_NAME,
    apiKey: CLOUDINARY_API_KEY,
    apiSecret: CLOUDINARY_API_SECRET
  };
};
var configureCloudinary = () => {
  const { cloudName, apiKey, apiSecret } = getCloudinaryConfig();
  console.log("Cloudinary Config:", {
    cloud_name: cloudName,
    api_key: apiKey ? "Exists (not shown)" : "Missing",
    api_secret: apiSecret ? "Exists (not shown)" : "Missing"
  });
  cloudinary.config({
    cloud_name: cloudName,
    api_key: apiKey,
    api_secret: apiSecret,
    secure: true
  });
  return { cloudName, apiKey, apiSecret };
};
configureCloudinary();
var verifyCloudinaryConfig = () => {
  const { cloudName, apiKey, apiSecret } = getCloudinaryConfig();
  if (!cloudName || !apiKey || !apiSecret) {
    log("Missing Cloudinary environment variables", "cloudinary");
    return false;
  }
  cloudinary.config({
    cloud_name: cloudName,
    api_key: apiKey,
    api_secret: apiSecret,
    secure: true
  });
  return true;
};
var uploadImage = async (file, folder = "godivatech/portfolio") => {
  if (!verifyCloudinaryConfig()) {
    throw new Error("Cloudinary configuration is missing");
  }
  try {
    if (file.startsWith("data:image")) {
      console.log("Uploading base64 image to Cloudinary folder:", folder);
      console.log("Image data length:", file.length);
      console.log("Image data type:", file.substring(0, 30) + "...");
      try {
        const result = await cloudinary.uploader.upload(file, {
          folder,
          resource_type: "auto"
        });
        console.log("Successfully uploaded to Cloudinary:", result.secure_url);
        return result.secure_url;
      } catch (uploadError) {
        console.error("Specific upload error:", uploadError.message);
        console.error("Error details:", uploadError);
        throw uploadError;
      }
    } else if (file.startsWith("http")) {
      if (file.includes("cloudinary.com")) {
        console.log("Image is already in Cloudinary:", file);
        return file;
      }
      console.log("Uploading URL to Cloudinary folder:", folder);
      try {
        const result = await cloudinary.uploader.upload(file, {
          folder,
          resource_type: "auto"
        });
        console.log("Successfully uploaded URL to Cloudinary:", result.secure_url);
        return result.secure_url;
      } catch (uploadError) {
        console.error("Specific URL upload error:", uploadError.message);
        console.error("Error details:", uploadError);
        throw uploadError;
      }
    }
    console.error("Unsupported image format. File starts with:", file.substring(0, 30));
    throw new Error("Unsupported image format");
  } catch (error) {
    console.error("Error uploading to Cloudinary:", error);
    throw error;
  }
};

// server/sitemap.ts
var baseUrl = "https://godivatech.com";
var formatDate = (date) => {
  if (typeof date === "string") {
    date = new Date(date);
  }
  return date.toISOString();
};
var generateSitemap = async () => {
  try {
    const [blogPosts2, categories2, services2, projects2] = await Promise.all([
      firestoreStorage.getAllBlogPosts(),
      firestoreStorage.getAllCategories(),
      firestoreStorage.getAllServices(),
      firestoreStorage.getAllProjects()
    ]);
    let xml = '<?xml version="1.0" encoding="UTF-8"?>\n';
    xml += '<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9" ';
    xml += 'xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" ';
    xml += 'xmlns:image="http://www.google.com/schemas/sitemap-image/1.1" ';
    xml += 'xmlns:video="http://www.google.com/schemas/sitemap-video/1.1" ';
    xml += 'xmlns:news="http://www.google.com/schemas/sitemap-news/0.9" ';
    xml += 'xmlns:mobile="http://www.google.com/schemas/sitemap-mobile/1.0" ';
    xml += 'xmlns:pagemap="http://www.google.com/schemas/sitemap-pagemap/1.0" ';
    xml += 'xmlns:xhtml="http://www.w3.org/1999/xhtml">\n';
    const locations = [
      { code: "en-IN", name: "Madurai" },
      { code: "ta-IN", name: "\u0BAE\u0BA4\u0BC1\u0BB0\u0BC8" }
      // Tamil version
    ];
    const staticPages = [
      { url: "/", priority: 1, changefreq: "weekly" },
      { url: "/about", priority: 0.8, changefreq: "monthly" },
      { url: "/services", priority: 0.9, changefreq: "weekly" },
      { url: "/portfolio", priority: 0.8, changefreq: "weekly" },
      { url: "/blog", priority: 0.8, changefreq: "daily" },
      { url: "/contact", priority: 0.7, changefreq: "monthly" },
      // Madurai neighborhood targeting for SEO
      { url: "/madurai/anna-nagar", priority: 0.7, changefreq: "monthly" },
      { url: "/madurai/iyer-bungalow", priority: 0.7, changefreq: "monthly" },
      { url: "/madurai/kk-nagar", priority: 0.7, changefreq: "monthly" },
      { url: "/madurai/ss-colony", priority: 0.7, changefreq: "monthly" },
      { url: "/madurai/tirunagar", priority: 0.7, changefreq: "monthly" },
      { url: "/tamil-nadu", priority: 0.6, changefreq: "monthly" }
    ];
    staticPages.forEach((page) => {
      xml += "  <url>\n";
      xml += `    <loc>${baseUrl}${page.url}</loc>
`;
      xml += `    <lastmod>${formatDate(/* @__PURE__ */ new Date())}</lastmod>
`;
      xml += `    <changefreq>${page.changefreq}</changefreq>
`;
      xml += `    <priority>${page.priority}</priority>
`;
      locations.forEach((location) => {
        xml += `    <xhtml:link rel="alternate" hreflang="${location.code}" href="${baseUrl}${page.url}" />
`;
      });
      xml += "    <mobile:mobile/>\n";
      xml += "  </url>\n";
    });
    blogPosts2.forEach((post) => {
      const postUrl = `/blog/${post.slug}`;
      const updateDate = post.publishedAt;
      xml += "  <url>\n";
      xml += `    <loc>${baseUrl}${postUrl}</loc>
`;
      xml += `    <lastmod>${formatDate(updateDate)}</lastmod>
`;
      xml += "    <changefreq>monthly</changefreq>\n";
      xml += "    <priority>0.7</priority>\n";
      xml += `    <xhtml:link rel="alternate" media="only screen and (max-width: 640px)" href="${baseUrl}${postUrl}?amp=1" />
`;
      xml += "    <mobile:mobile/>\n";
      if (post.coverImage) {
        xml += "    <image:image>\n";
        xml += `      <image:loc>${post.coverImage}</image:loc>
`;
        xml += `      <image:title>${post.title} | GodivaTech Madurai</image:title>
`;
        xml += `      <image:caption>${post.excerpt}</image:caption>
`;
        xml += "    </image:image>\n";
      }
      xml += "  </url>\n";
    });
    categories2.forEach((category) => {
      const categoryUrl = `/blog/category/${category.slug}`;
      xml += "  <url>\n";
      xml += `    <loc>${baseUrl}${categoryUrl}</loc>
`;
      xml += `    <lastmod>${formatDate(/* @__PURE__ */ new Date())}</lastmod>
`;
      xml += "    <changefreq>weekly</changefreq>\n";
      xml += "    <priority>0.6</priority>\n";
      xml += "    <mobile:mobile/>\n";
      xml += "  </url>\n";
    });
    services2.forEach((service) => {
      const serviceUrl = `/services/${service.slug}`;
      xml += "  <url>\n";
      xml += `    <loc>${baseUrl}${serviceUrl}</loc>
`;
      xml += `    <lastmod>${formatDate(/* @__PURE__ */ new Date())}</lastmod>
`;
      xml += "    <changefreq>monthly</changefreq>\n";
      xml += "    <priority>0.8</priority>\n";
      locations.forEach((location) => {
        xml += `    <xhtml:link rel="alternate" hreflang="${location.code}" href="${baseUrl}${serviceUrl}" />
`;
      });
      xml += "    <mobile:mobile/>\n";
      if (service.icon) {
        xml += "    <image:image>\n";
        xml += `      <image:loc>${service.icon}</image:loc>
`;
        xml += `      <image:title>${service.title} in Madurai | GodivaTech</image:title>
`;
        xml += `      <image:caption>Professional ${service.title} services in Madurai and Tamil Nadu</image:caption>
`;
        xml += "    </image:image>\n";
      }
      xml += "  </url>\n";
      ["anna-nagar", "iyer-bungalow", "kk-nagar", "ss-colony", "tirunagar"].forEach((area) => {
        const areaServiceUrl = `/services/${service.slug}/madurai/${area}`;
        xml += "  <url>\n";
        xml += `    <loc>${baseUrl}${areaServiceUrl}</loc>
`;
        xml += `    <lastmod>${formatDate(/* @__PURE__ */ new Date())}</lastmod>
`;
        xml += "    <changefreq>monthly</changefreq>\n";
        xml += "    <priority>0.7</priority>\n";
        xml += "    <mobile:mobile/>\n";
        xml += "  </url>\n";
      });
      const tnServiceUrl = `/services/${service.slug}/tamil-nadu`;
      xml += "  <url>\n";
      xml += `    <loc>${baseUrl}${tnServiceUrl}</loc>
`;
      xml += `    <lastmod>${formatDate(/* @__PURE__ */ new Date())}</lastmod>
`;
      xml += "    <changefreq>monthly</changefreq>\n";
      xml += "    <priority>0.6</priority>\n";
      xml += "    <mobile:mobile/>\n";
      xml += "  </url>\n";
    });
    projects2.forEach((project) => {
      const slug = project.title?.toLowerCase().replace(/\s+/g, "-").replace(/[^a-z0-9-]/g, "") || "";
      if (!slug) return;
      const projectUrl = `/portfolio/${slug}`;
      xml += "  <url>\n";
      xml += `    <loc>${baseUrl}${projectUrl}</loc>
`;
      xml += `    <lastmod>${formatDate(/* @__PURE__ */ new Date())}</lastmod>
`;
      xml += "    <changefreq>monthly</changefreq>\n";
      xml += "    <priority>0.6</priority>\n";
      xml += "    <mobile:mobile/>\n";
      if (project.image) {
        xml += "    <image:image>\n";
        xml += `      <image:loc>${project.image}</image:loc>
`;
        xml += `      <image:title>${project.title} | GodivaTech Madurai Portfolio</image:title>
`;
        xml += `      <image:caption>${project.description || project.title}</image:caption>
`;
        xml += "    </image:image>\n";
      }
      xml += "  </url>\n";
    });
    xml += "</urlset>";
    return xml;
  } catch (error) {
    console.error("Error generating sitemap:", error);
    return `<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>${baseUrl}/</loc>
    <lastmod>${formatDate(/* @__PURE__ */ new Date())}</lastmod>
    <priority>1.0</priority>
  </url>
  <url>
    <loc>${baseUrl}/about</loc>
    <lastmod>${formatDate(/* @__PURE__ */ new Date())}</lastmod>
    <priority>0.8</priority>
  </url>
  <url>
    <loc>${baseUrl}/services</loc>
    <lastmod>${formatDate(/* @__PURE__ */ new Date())}</lastmod>
    <priority>0.9</priority>
  </url>
  <url>
    <loc>${baseUrl}/portfolio</loc>
    <lastmod>${formatDate(/* @__PURE__ */ new Date())}</lastmod>
    <priority>0.8</priority>
  </url>
  <url>
    <loc>${baseUrl}/blog</loc>
    <lastmod>${formatDate(/* @__PURE__ */ new Date())}</lastmod>
    <priority>0.8</priority>
  </url>
  <url>
    <loc>${baseUrl}/contact</loc>
    <lastmod>${formatDate(/* @__PURE__ */ new Date())}</lastmod>
    <priority>0.7</priority>
  </url>
</urlset>`;
  }
};
var setupSitemap = (app3) => {
  app3.get("/sitemap.xml", async (_req, res) => {
    try {
      const sitemap = await generateSitemap();
      res.header("Content-Type", "application/xml");
      res.send(sitemap);
    } catch (error) {
      console.error("Error serving sitemap:", error);
      res.status(500).send("Error generating sitemap");
    }
  });
};

// server/routes.ts
async function registerRoutes(app3) {
  setupAuth(app3);
  setupSitemap(app3);
  app3.get("/api/health", (req, res) => {
    res.json({
      status: "ok",
      timestamp: (/* @__PURE__ */ new Date()).toISOString(),
      environment: process.env.NODE_ENV || "development"
    });
  });
  app3.get("/api/ping", (req, res) => {
    res.json({
      status: "ok",
      timestamp: (/* @__PURE__ */ new Date()).toISOString(),
      server: "GodivaTech API"
    });
  });
  app3.head("/api/ping", (req, res) => {
    res.setHeader("X-Server-Time", (/* @__PURE__ */ new Date()).toISOString());
    res.setHeader("X-Server", "GodivaTech API");
    res.status(200).end();
  });
  if (process.env.NODE_ENV !== "production") {
    app3.get("/api/debug", (req, res) => {
      res.json({
        nodeEnv: process.env.NODE_ENV,
        hasFirebaseConfig: !!process.env.FIREBASE_PROJECT_ID,
        hasCloudinary: !!process.env.CLOUDINARY_CLOUD_NAME,
        allowedOrigins: process.env.ALLOWED_ORIGINS || "not set"
      });
    });
  }
  app3.post("/api/upload", isAuthenticated, async (req, res) => {
    try {
      const { image, folder } = req.body;
      if (!image) {
        return res.status(400).json({ message: "No image provided" });
      }
      const uploadedUrl = await uploadImage(image, folder || "godivatech/portfolio");
      res.json({ url: uploadedUrl });
    } catch (error) {
      console.error("Error uploading image:", error);
      res.status(500).json({ message: "Failed to upload image" });
    }
  });
  app3.get("/api/blog-posts", async (req, res) => {
    try {
      const blogPosts2 = await storage.getAllBlogPosts();
      const postsWithCategories = await Promise.all(
        blogPosts2.map(async (post) => {
          if (post.categoryId) {
            const category = await storage.getCategory(post.categoryId);
            return { ...post, category };
          }
          return post;
        })
      );
      res.json(postsWithCategories);
    } catch (error) {
      console.error("Error fetching blog posts:", error);
      res.status(500).json({ message: "Failed to fetch blog posts" });
    }
  });
  app3.get("/api/blog-posts/:slug", async (req, res) => {
    try {
      const slug = req.params.slug;
      const blogPost = await storage.getBlogPostBySlug(slug);
      if (!blogPost) {
        return res.status(404).json({ message: "Blog post not found" });
      }
      if (blogPost.categoryId) {
        const category = await storage.getCategory(blogPost.categoryId);
        return res.json({ ...blogPost, category });
      }
      res.json(blogPost);
    } catch (error) {
      console.error("Error fetching blog post:", error);
      res.status(500).json({ message: "Failed to fetch blog post" });
    }
  });
  app3.get("/api/categories", async (req, res) => {
    try {
      const categories2 = await storage.getAllCategories();
      res.json(categories2);
    } catch (error) {
      console.error("Error fetching categories:", error);
      res.status(500).json({ message: "Failed to fetch categories" });
    }
  });
  app3.get("/api/services", async (req, res) => {
    try {
      const services2 = await storage.getAllServices();
      res.json(services2);
    } catch (error) {
      console.error("Error fetching services:", error);
      res.status(500).json({ message: "Failed to fetch services" });
    }
  });
  app3.get("/api/services/:slug", async (req, res) => {
    try {
      const slug = req.params.slug;
      const service = await storage.getServiceBySlug(slug);
      if (!service) {
        return res.status(404).json({ message: "Service not found" });
      }
      res.json(service);
    } catch (error) {
      console.error("Error fetching service:", error);
      res.status(500).json({ message: "Failed to fetch service" });
    }
  });
  app3.get("/api/team-members", async (req, res) => {
    try {
      const teamMembers2 = await storage.getAllTeamMembers();
      res.json(teamMembers2);
    } catch (error) {
      console.error("Error fetching team members:", error);
      res.status(500).json({ message: "Failed to fetch team members" });
    }
  });
  app3.get("/api/projects", async (req, res) => {
    try {
      const projects2 = await storage.getAllProjects();
      res.json(projects2);
    } catch (error) {
      console.error("Error fetching projects:", error);
      res.status(500).json({ message: "Failed to fetch projects" });
    }
  });
  app3.get("/api/testimonials", async (req, res) => {
    try {
      const testimonials2 = await storage.getAllTestimonials();
      res.json(testimonials2);
    } catch (error) {
      console.error("Error fetching testimonials:", error);
      res.status(500).json({ message: "Failed to fetch testimonials" });
    }
  });
  app3.post("/api/contact", async (req, res) => {
    try {
      const validatedData = insertContactMessageSchema.parse(req.body);
      const contactMessage = await storage.createContactMessage(validatedData);
      res.status(201).json(contactMessage);
    } catch (error) {
      if (error instanceof z.ZodError) {
        return res.status(400).json({ message: "Invalid form data", errors: error.errors });
      }
      console.error("Error submitting contact form:", error);
      res.status(500).json({ message: "Failed to submit contact form" });
    }
  });
  app3.post("/api/subscribe", async (req, res) => {
    try {
      const validatedData = insertSubscriberSchema.parse(req.body);
      const existingSubscriber = await storage.getSubscriberByEmail(validatedData.email);
      if (existingSubscriber) {
        return res.status(400).json({ message: "Email already subscribed" });
      }
      const subscriber = await storage.createSubscriber(validatedData);
      res.status(201).json(subscriber);
    } catch (error) {
      if (error instanceof z.ZodError) {
        return res.status(400).json({ message: "Invalid email", errors: error.errors });
      }
      console.error("Error subscribing to newsletter:", error);
      res.status(500).json({ message: "Failed to subscribe to newsletter" });
    }
  });
  app3.get("/api/admin/blog-posts", isAuthenticated, async (req, res) => {
    try {
      const posts = await storage.getAllBlogPosts();
      res.json(posts);
    } catch (error) {
      console.error("Error fetching blog posts:", error);
      res.status(500).json({ message: "Failed to fetch blog posts" });
    }
  });
  app3.post("/api/admin/blog-posts", isAuthenticated, async (req, res) => {
    try {
      const validatedData = insertBlogPostSchema.parse(req.body);
      const post = await storage.createBlogPost(validatedData);
      res.status(201).json(post);
    } catch (error) {
      if (error instanceof z.ZodError) {
        return res.status(400).json({ message: "Invalid data", errors: error.errors });
      }
      console.error("Error creating blog post:", error);
      res.status(500).json({ message: "Failed to create blog post" });
    }
  });
  app3.put("/api/admin/blog-posts/:id", isAuthenticated, async (req, res) => {
    try {
      const id = parseInt(req.params.id);
      const post = await storage.updateBlogPost(id, req.body);
      if (!post) {
        return res.status(404).json({ message: "Blog post not found" });
      }
      res.json(post);
    } catch (error) {
      console.error("Error updating blog post:", error);
      res.status(500).json({ message: "Failed to update blog post" });
    }
  });
  app3.delete("/api/admin/blog-posts/:id", isAuthenticated, async (req, res) => {
    try {
      const id = parseInt(req.params.id);
      await storage.deleteBlogPost(id);
      res.status(204).end();
    } catch (error) {
      console.error("Error deleting blog post:", error);
      res.status(500).json({ message: "Failed to delete blog post" });
    }
  });
  app3.get("/api/admin/categories", isAuthenticated, async (req, res) => {
    try {
      const categories2 = await storage.getAllCategories();
      res.json(categories2);
    } catch (error) {
      console.error("Error fetching categories:", error);
      res.status(500).json({ message: "Failed to fetch categories" });
    }
  });
  app3.post("/api/admin/categories", isAuthenticated, async (req, res) => {
    try {
      const validatedData = insertCategorySchema.parse(req.body);
      const category = await storage.createCategory(validatedData);
      res.status(201).json(category);
    } catch (error) {
      if (error instanceof z.ZodError) {
        return res.status(400).json({ message: "Invalid data", errors: error.errors });
      }
      console.error("Error creating category:", error);
      res.status(500).json({ message: "Failed to create category" });
    }
  });
  app3.put("/api/admin/categories/:id", isAuthenticated, async (req, res) => {
    try {
      const id = parseInt(req.params.id);
      const category = await storage.updateCategory(id, req.body);
      if (!category) {
        return res.status(404).json({ message: "Category not found" });
      }
      res.json(category);
    } catch (error) {
      console.error("Error updating category:", error);
      res.status(500).json({ message: "Failed to update category" });
    }
  });
  app3.delete("/api/admin/categories/:id", isAuthenticated, async (req, res) => {
    try {
      const id = parseInt(req.params.id);
      await storage.deleteCategory(id);
      res.status(204).end();
    } catch (error) {
      console.error("Error deleting category:", error);
      res.status(500).json({ message: "Failed to delete category" });
    }
  });
  app3.get("/api/admin/services", isAuthenticated, async (req, res) => {
    try {
      const services2 = await storage.getAllServices();
      res.json(services2);
    } catch (error) {
      console.error("Error fetching services:", error);
      res.status(500).json({ message: "Failed to fetch services" });
    }
  });
  app3.post("/api/admin/services", isAuthenticated, async (req, res) => {
    try {
      const validatedData = insertServiceSchema.parse(req.body);
      const service = await storage.createService(validatedData);
      res.status(201).json(service);
    } catch (error) {
      if (error instanceof z.ZodError) {
        return res.status(400).json({ message: "Invalid data", errors: error.errors });
      }
      console.error("Error creating service:", error);
      res.status(500).json({ message: "Failed to create service" });
    }
  });
  app3.put("/api/admin/services/:id", isAuthenticated, async (req, res) => {
    try {
      const id = parseInt(req.params.id);
      const service = await storage.updateService(id, req.body);
      if (!service) {
        return res.status(404).json({ message: "Service not found" });
      }
      res.json(service);
    } catch (error) {
      console.error("Error updating service:", error);
      res.status(500).json({ message: "Failed to update service" });
    }
  });
  app3.delete("/api/admin/services/:id", isAuthenticated, async (req, res) => {
    try {
      const id = parseInt(req.params.id);
      await storage.deleteService(id);
      res.status(204).end();
    } catch (error) {
      console.error("Error deleting service:", error);
      res.status(500).json({ message: "Failed to delete service" });
    }
  });
  app3.get("/api/admin/team-members", isAuthenticated, async (req, res) => {
    try {
      const members = await storage.getAllTeamMembers();
      res.json(members);
    } catch (error) {
      console.error("Error fetching team members:", error);
      res.status(500).json({ message: "Failed to fetch team members" });
    }
  });
  app3.post("/api/admin/team-members", isAuthenticated, async (req, res) => {
    try {
      const validatedData = insertTeamMemberSchema.parse(req.body);
      const member = await storage.createTeamMember(validatedData);
      res.status(201).json(member);
    } catch (error) {
      if (error instanceof z.ZodError) {
        return res.status(400).json({ message: "Invalid data", errors: error.errors });
      }
      console.error("Error creating team member:", error);
      res.status(500).json({ message: "Failed to create team member" });
    }
  });
  app3.put("/api/admin/team-members/:id", isAuthenticated, async (req, res) => {
    try {
      const id = parseInt(req.params.id);
      const member = await storage.updateTeamMember(id, req.body);
      if (!member) {
        return res.status(404).json({ message: "Team member not found" });
      }
      res.json(member);
    } catch (error) {
      console.error("Error updating team member:", error);
      res.status(500).json({ message: "Failed to update team member" });
    }
  });
  app3.delete("/api/admin/team-members/:id", isAuthenticated, async (req, res) => {
    try {
      const id = parseInt(req.params.id);
      await storage.deleteTeamMember(id);
      res.status(204).end();
    } catch (error) {
      console.error("Error deleting team member:", error);
      res.status(500).json({ message: "Failed to delete team member" });
    }
  });
  app3.get("/api/admin/projects", isAuthenticated, async (req, res) => {
    try {
      const projects2 = await storage.getAllProjects();
      res.json(projects2);
    } catch (error) {
      console.error("Error fetching projects:", error);
      res.status(500).json({ message: "Failed to fetch projects" });
    }
  });
  app3.post("/api/admin/projects", isAuthenticated, async (req, res) => {
    try {
      console.log("Project creation request received:", req.body);
      const validatedData = insertProjectSchema.parse(req.body);
      const existingProjects = await storage.getAllProjects();
      console.log(`Checking against ${existingProjects.length} existing projects`);
      const isDuplicate = existingProjects.some(
        (existingProject) => existingProject.title && validatedData.title && existingProject.title.toLowerCase() === validatedData.title.toLowerCase()
      );
      if (isDuplicate) {
        console.log(`Duplicate project detected: "${validatedData.title}"`);
        return res.status(409).json({
          message: "A project with this title already exists",
          status: "duplicate"
        });
      }
      console.log("Creating new project:", validatedData);
      const project = await storage.createProject(validatedData);
      if (wss) {
        try {
          const message = {
            type: "project_created",
            data: project
          };
          console.log(`Broadcasting project_created event to ${wss.clients.size} clients`);
          wss.clients.forEach((client) => {
            if (client.readyState === WebSocket.OPEN) {
              client.send(JSON.stringify(message));
            }
          });
        } catch (wsError) {
          console.error("Error broadcasting project creation:", wsError);
        }
      }
      console.log("Project created successfully:", project);
      res.status(201).json(project);
    } catch (error) {
      if (error instanceof z.ZodError) {
        return res.status(400).json({ message: "Invalid data", errors: error.errors });
      }
      console.error("Error creating project:", error);
      res.status(500).json({ message: "Failed to create project" });
    }
  });
  app3.get("/api/admin/testimonials", isAuthenticated, async (req, res) => {
    try {
      const testimonials2 = await storage.getAllTestimonials();
      res.json(testimonials2);
    } catch (error) {
      console.error("Error fetching testimonials:", error);
      res.status(500).json({ message: "Failed to fetch testimonials" });
    }
  });
  app3.post("/api/admin/testimonials", isAuthenticated, async (req, res) => {
    try {
      const validatedData = insertTestimonialSchema.parse(req.body);
      const testimonial = await storage.createTestimonial(validatedData);
      res.status(201).json(testimonial);
    } catch (error) {
      if (error instanceof z.ZodError) {
        return res.status(400).json({ message: "Invalid data", errors: error.errors });
      }
      console.error("Error creating testimonial:", error);
      res.status(500).json({ message: "Failed to create testimonial" });
    }
  });
  app3.put("/api/admin/testimonials/:id", isAuthenticated, async (req, res) => {
    try {
      const id = parseInt(req.params.id);
      const testimonial = await storage.updateTestimonial(id, req.body);
      if (!testimonial) {
        return res.status(404).json({ message: "Testimonial not found" });
      }
      res.json(testimonial);
    } catch (error) {
      console.error("Error updating testimonial:", error);
      res.status(500).json({ message: "Failed to update testimonial" });
    }
  });
  app3.delete("/api/admin/testimonials/:id", isAuthenticated, async (req, res) => {
    try {
      const id = parseInt(req.params.id);
      await storage.deleteTestimonial(id);
      res.status(204).end();
    } catch (error) {
      console.error("Error deleting testimonial:", error);
      res.status(500).json({ message: "Failed to delete testimonial" });
    }
  });
  app3.get("/api/admin/contact-messages", isAuthenticated, async (req, res) => {
    try {
      const messages = await storage.getAllContactMessages();
      res.json(messages);
    } catch (error) {
      console.error("Error fetching contact messages:", error);
      res.status(500).json({ message: "Failed to fetch contact messages" });
    }
  });
  app3.delete("/api/admin/contact-messages/:id", isAuthenticated, async (req, res) => {
    try {
      const id = parseInt(req.params.id);
      await storage.deleteContactMessage(id);
      res.status(204).end();
    } catch (error) {
      console.error("Error deleting contact message:", error);
      res.status(500).json({ message: "Failed to delete contact message" });
    }
  });
  app3.get("/api/admin/subscribers", isAuthenticated, async (req, res) => {
    try {
      const subscribers2 = await storage.getAllSubscribers();
      res.json(subscribers2);
    } catch (error) {
      console.error("Error fetching subscribers:", error);
      res.status(500).json({ message: "Failed to fetch subscribers" });
    }
  });
  app3.delete("/api/admin/subscribers/:id", isAuthenticated, async (req, res) => {
    try {
      const id = parseInt(req.params.id);
      await storage.deleteSubscriber(id);
      res.status(204).end();
    } catch (error) {
      console.error("Error deleting subscriber:", error);
      res.status(500).json({ message: "Failed to delete subscriber" });
    }
  });
  const httpServer = createServer(app3);
  console.log("Setting up WebSocket server on path: /ws");
  const wss = new WebSocketServer({
    server: httpServer,
    path: "/ws",
    // Add more options for debugging
    clientTracking: true,
    perMessageDeflate: false
  });
  wss.on("connection", (ws, req) => {
    const clientIp = req.socket.remoteAddress;
    const url = req.url || "unknown";
    console.log(`Client connected to WebSocket from ${clientIp}, URL: ${url}`);
    ws.send(JSON.stringify({
      type: "connection",
      message: "Connected to GodivaTech WebSocket server",
      timestamp: (/* @__PURE__ */ new Date()).toISOString()
    }));
    ws.on("message", (message) => {
      try {
        const parsedMessage = JSON.parse(message.toString());
        console.log(`Received WebSocket message:`, parsedMessage);
      } catch (error) {
        console.error("Error parsing WebSocket message:", error);
      }
    });
    ws.on("close", (code, reason) => {
      console.log(`Client disconnected from WebSocket with code ${code}${reason ? `, reason: ${reason}` : ""}`);
    });
    ws.on("error", (error) => {
      console.error("WebSocket connection error:", error);
    });
  });
  wss.on("error", (error) => {
    console.error("WebSocket server error:", error);
  });
  const broadcastUpdate = (type, data) => {
    console.log(`Broadcasting ${type} update to ${wss.clients.size} clients`);
    wss.clients.forEach((client) => {
      if (client.readyState === WebSocket.OPEN) {
        client.send(JSON.stringify({
          type,
          data,
          timestamp: (/* @__PURE__ */ new Date()).toISOString()
        }));
      }
    });
  };
  app3.delete("/api/admin/projects/:id", isAuthenticated, async (req, res) => {
    try {
      console.log(`=== PROJECT DELETION OPERATION ===`);
      console.log(`Received delete request for project with ID: ${req.params.id} (type: ${typeof req.params.id})`);
      if (!req.params.id || req.params.id === "null" || req.params.id === "undefined") {
        console.error(`Invalid project ID for deletion: ${req.params.id}`);
        return res.status(400).json({
          message: "Invalid project ID provided for deletion",
          received: req.params.id
        });
      }
      let id = req.params.id;
      const originalId = req.params.id;
      console.log(`Original ID from request: '${originalId}', type: ${typeof originalId}`);
      try {
        console.log(`Attempting to find project in database...`);
        const allProjects = await storage.getAllProjects();
        console.log(`Found ${allProjects.length} projects in storage`);
        if (allProjects.length === 0) {
          console.warn("No projects found in database");
          return res.status(404).json({
            message: "No projects found in database",
            id: originalId
          });
        }
        const matchingProject = allProjects.find((p) => {
          const stringMatch = p.id !== void 0 && String(p.id) === String(id);
          const numericMatch = p.id !== void 0 && typeof p.id === "number" && p.id === Number(id);
          const objectMatch = p.id !== void 0 && typeof p.id === "object" && p.id !== null && p.id._key !== void 0 && p.id._key === id;
          const firestoreMatch = p.docId !== void 0 && p.docId === id;
          return stringMatch || numericMatch || objectMatch || firestoreMatch;
        });
        if (matchingProject) {
          console.log(`Found matching project:`, {
            id: matchingProject.id,
            title: matchingProject.title,
            docId: "docId" in matchingProject ? matchingProject.docId : void 0
          });
          let specificId = matchingProject.id;
          let docId = "docId" in matchingProject ? matchingProject.docId : null;
          if (docId) {
            console.log(`Attempting to delete project with document ID: ${docId}`);
            const docSuccess = await storage.deleteProject(docId);
            if (docSuccess) {
              console.log(`Successfully deleted project with document ID: ${docId}`);
              broadcastUpdate("project_deleted", { id: specificId, docId });
              return res.status(200).json({
                message: "Project successfully deleted",
                id: specificId,
                docId
              });
            }
          }
          console.log(`Attempting to delete project with ID: ${specificId} (type: ${typeof specificId})`);
          const idSuccess = await storage.deleteProject(specificId);
          if (idSuccess) {
            console.log(`Successfully deleted project with ID: ${specificId}`);
            broadcastUpdate("project_deleted", { id: specificId });
            return res.status(200).json({
              message: "Project successfully deleted",
              id: specificId
            });
          }
          console.log(`Attempting to delete project with original ID: ${originalId}`);
          const originalSuccess = await storage.deleteProject(originalId);
          if (originalSuccess) {
            console.log(`Successfully deleted project with original ID: ${originalId}`);
            broadcastUpdate("project_deleted", { id: originalId });
            return res.status(200).json({
              message: "Project successfully deleted",
              id: originalId
            });
          }
          console.error(`Failed to delete project after multiple attempts`);
          return res.status(500).json({
            message: "Failed to delete project after multiple attempts",
            id: originalId
          });
        } else {
          console.warn(`No matching project found with ID: ${id}`);
          return res.status(404).json({
            message: "Project not found",
            id: originalId
          });
        }
      } catch (searchError) {
        console.error(`Error when searching for project:`, searchError);
        return res.status(500).json({
          message: "Error searching for project",
          error: searchError instanceof Error ? searchError.message : String(searchError)
        });
      }
    } catch (error) {
      console.error("Error in project deletion route:", error);
      return res.status(500).json({
        message: "Server error during project deletion",
        error: error instanceof Error ? error.message : String(error)
      });
    }
  });
  app3.post("/api/admin/projects", isAuthenticated, async (req, res) => {
    try {
      console.log(`=== PROJECT CREATION OPERATION ===`);
      const validatedData = insertProjectSchema.parse(req.body);
      console.log(`Creating new project with title: "${validatedData.title}"`);
      const project = await storage.createProject(validatedData);
      console.log(`Successfully created project with ID: ${project.id}, docId: ${project.docId || "N/A"}`);
      console.log(`Broadcasting project_created event to connected clients`);
      try {
        broadcastUpdate("project_created", project);
      } catch (wsError) {
        console.error("Error broadcasting project creation:", wsError);
      }
      res.status(201).json(project);
    } catch (error) {
      if (error instanceof z.ZodError) {
        console.error("Validation error creating project:", error.errors);
        return res.status(400).json({
          message: "Invalid project data",
          errors: error.errors
        });
      }
      console.error("Error creating project:", error);
      res.status(500).json({
        message: "Failed to create project",
        error: error instanceof Error ? error.message : String(error)
      });
    }
  });
  app3.put("/api/admin/projects/:id", isAuthenticated, async (req, res) => {
    try {
      console.log(`=== PROJECT UPDATE OPERATION ===`);
      console.log(`Received update request for project with ID: ${req.params.id} (type: ${typeof req.params.id})`);
      const originalId = req.params.id;
      if (!req.params.id || req.params.id === "null" || req.params.id === "undefined") {
        console.error(`Invalid project ID for update: ${req.params.id}`);
        return res.status(400).json({
          message: "Invalid project ID provided for update",
          received: req.params.id
        });
      }
      try {
        console.log(`Attempting to find project in database...`);
        const allProjects = await storage.getAllProjects();
        console.log(`Found ${allProjects.length} projects in storage for update operation`);
        if (allProjects.length === 0) {
          console.warn("No projects found in database");
          return res.status(404).json({
            message: "No projects found in database",
            id: originalId
          });
        }
        const matchingProject = allProjects.find((p) => {
          const stringMatch = p.id !== void 0 && String(p.id) === String(originalId);
          const numericMatch = p.id !== void 0 && typeof p.id === "number" && !isNaN(Number(originalId)) && p.id === Number(originalId);
          const firestoreMatch = p.docId !== void 0 && p.docId === originalId;
          return stringMatch || numericMatch || firestoreMatch;
        });
        if (matchingProject) {
          console.log(`Found matching project for update:`, {
            id: matchingProject.id,
            title: matchingProject.title,
            docId: "docId" in matchingProject ? matchingProject.docId : void 0
          });
          let updateId = originalId;
          if ("docId" in matchingProject && matchingProject.docId) {
            console.log(`Using Firebase document ID for update: ${matchingProject.docId}`);
            updateId = matchingProject.docId;
          }
          console.log(`Attempting to update project with final ID: ${updateId}`);
          const project = await storage.updateProject(updateId, req.body);
          if (!project) {
            console.warn(`Project with ID ${updateId} not found or could not be updated`);
            return res.status(404).json({
              message: "Project not found or could not be updated",
              id: updateId
            });
          }
          console.log(`Successfully updated project with ID: ${updateId}, broadcasting update`);
          broadcastUpdate("project_updated", project);
          res.json(project);
        } else {
          console.warn(`No matching project found with ID: ${originalId} for update`);
          return res.status(404).json({
            message: "Project not found",
            id: originalId
          });
        }
      } catch (searchError) {
        console.error(`Error when searching for project:`, searchError);
        return res.status(500).json({
          message: "Error searching for project",
          error: searchError instanceof Error ? searchError.message : String(searchError)
        });
      }
    } catch (error) {
      console.error("Error in project update route:", error);
      return res.status(500).json({
        message: "Server error during project update",
        error: error instanceof Error ? error.message : String(error)
      });
    }
  });
  return httpServer;
}

// server/index.ts
dotenv.config();
var app2 = express2();
var allowedOrigins;
if (process.env.NODE_ENV === "production") {
  if (process.env.ALLOWED_ORIGINS) {
    allowedOrigins = process.env.ALLOWED_ORIGINS.split(",").map((origin) => origin.trim());
    console.log("Allowed origins from env:", allowedOrigins);
  } else {
    allowedOrigins = [
      "https://godivatech.vercel.app",
      // Main production frontend
      "https://godiva-tech.vercel.app",
      // Alternative production domain
      /\.vercel\.app$/
      // Allow all Vercel preview deployments
    ];
  }
} else {
  allowedOrigins = "*";
}
app2.use(cors({
  origin: allowedOrigins,
  credentials: true,
  methods: ["GET", "POST", "PUT", "DELETE", "OPTIONS", "PATCH"],
  allowedHeaders: ["Content-Type", "Authorization", "Accept"]
}));
app2.use(express2.json({ limit: "10mb" }));
app2.use(express2.urlencoded({ extended: false, limit: "10mb" }));
app2.use((req, res, next) => {
  const start = Date.now();
  const path3 = req.path;
  let capturedJsonResponse = void 0;
  const originalResJson = res.json;
  res.json = function(bodyJson, ...args) {
    capturedJsonResponse = bodyJson;
    return originalResJson.apply(res, [bodyJson, ...args]);
  };
  res.on("finish", () => {
    const duration = Date.now() - start;
    if (path3.startsWith("/api")) {
      let logLine = `${req.method} ${path3} ${res.statusCode} in ${duration}ms`;
      if (capturedJsonResponse) {
        logLine += ` :: ${JSON.stringify(capturedJsonResponse)}`;
      }
      if (logLine.length > 80) {
        logLine = logLine.slice(0, 79) + "\u2026";
      }
      log(logLine);
    }
  });
  next();
});
(async () => {
  const server = await registerRoutes(app2);
  app2.use((err, _req, res, _next) => {
    const status = err.status || err.statusCode || 500;
    const message = err.message || "Internal Server Error";
    res.status(status).json({ message });
    throw err;
  });
  if (app2.get("env") === "development") {
    await setupVite(app2, server);
  } else {
    serveStatic(app2);
  }
  const port = 5e3;
  server.listen({
    port,
    host: "0.0.0.0",
    reusePort: true
  }, () => {
    log(`serving on port ${port}`);
  });
})();